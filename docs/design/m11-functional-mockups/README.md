# M11 功能项 mockups

M11（ADR-0025 重定义后共 7 项）里只有三处会产生用户可见 UI，本目录一页覆盖，其余任务
（fuses、秘密扫描、性能 fixture、虚拟化、门槛报告）为测试与硬化，无 UI 不出稿。

- `index.html` — 单页三段，全部可交互：
  1. **隐私设置**（M11-07 / PRIV-001..003）：分析开启前字段清单弹窗、崩溃上报脱敏预览、
     本地数据面板（位置/占用明细/保留策略/两段确认删除）。
  2. **UI 缩放**（M11-05 / A11Y-003）：Settings → General 档位 80–200% + 活预览，
     ≥150% 演示画布堆叠断点（重排而非裁切）。
  3. **无障碍 Diff**（M11-05 / A11Y-005）：Diff 页签「并排 ⇄ 文本模式」切换，
     变更卡片线性化 + F7/⇧F7 逐变更导航 + 可视化 ARIA-LIVE 播报条。

样式按 Archive 皮肤 token 手写；实现时走既有 theme 变量，三皮肤/亮暗自动跟随。

## 待用户确认的开放点（页内右侧橙色「待确认」卡）

1. 删除历史是否包含已归档任务（mock 按包含）。
2. 200% 缩放下 Session Rail 默认保持展开还是折叠成图标条（mock 按展开）。
3. 检测到 VoiceOver 时是否自动切文本模式（mock 按手动）。

状态：2026-07-18 出稿，等浏览器确认后实现（顺序 M11-07 → M11-05）。
