# IMPLEMENTATION_STATUS.md

Overall status: IN_PROGRESS  
Current milestone: v1.0.0-beta.6 unsigned GitHub Prerelease CANDIDATE; Mission orchestration, extensible Agents and Terminal Replay VERIFIED; signed Stable is BLOCKED
Last verified commit: (see git log)

## Status legend

- NOT_STARTED
- IN_PROGRESS
- BLOCKED
- DONE
- VERIFIED

## Milestones

| Milestone | Status | Exit evidence | Notes |
| --- | --- | --- | --- |
| SCM-DIFF. SCM diff 进编辑器 + 壳内浮层层级修复（ADR-0057；user report 2026-08-09 截图：点未跟踪文件弹出的巨型 diff 模态被 Session rail 压住半边、标题不可见、新文件左右分栏半屏斜纹）— 删除 M5 遗留 GitDiffModal；变更行点击 → 相邻编辑器内打开 diff TAB（工作树 diff 右侧=可编辑实时 buffer（⌘S/autosave/dirty 与文件 tab 同一文档路径）、staged=HEAD↔index 只读、新文件默认行内视图、±行数统计、git.changed 自动刷新、untracked/conflict 直接开文件）；tab 模型升级为 file\|diff 双态（id 键控，file tab id=path 兼容全部旧调用点；持久化 schema 加 kind/staged 向后兼容）；根因修复：`.hm-root` 是 z-index:0 堆叠上下文而 `.sr-rail` z-index:4，壳内一切 fixed 浮层永远画在 rail 之下 → 确立"壳内全屏浮层 portal 到 body"纪律并应用到 DiscardConfirm/CompareOverlay/CloseDialog/FileLens/NewProjectDialog/NewTaskDialog/终端创建与 kill 对话框/搜索替换预览；SCM 行 hover 显操作、丢弃按钮危险色、全面 i18n | DONE (landed) | `npm run check` clean（Prettier + boundary 582 + tsc）；vitest 179 files / 1422 tests 含 NEW `editor-tabs.test.ts` 11（id 编解码/同路径三 tab 区分/文档持有判定/关闭回退/持久化映射）；e2e `m5-git.spec.ts` 2/2 重写（diff tab 打开+关闭、staged tab、untracked 目录内文件→文件 tab、原有 stage/unstage/commit/branch CLI 对照全保留；顺带修复该 spec 未跟上 ADR-0054 boot-on-Home 的预存失效导航）；验收追加修复：`git status` 加 `--untracked-files=all`（默认把未跟踪目录折叠成 `examples/` 一行，点击报"无法读取文件"）+ 渲染层拒绝 `dir/` 进 doc.open + git.test.ts 新增枚举断言（16/16）；验收第二轮三修：① SCM 行悬浮恢复显示完整路径（行文本本就截断）② Search/Changes 列宽可拖动（`.ptc-resize` 同 rail 交互语言：拖动/方向键/Home/双击重置，localStorage 持久化 200–640px，窗口缩放动态钳制编辑器 ≥420px，删除旧 256px 窄屏固定覆盖）③ commit 后树标记不清除的双根因（gitStatusStore 未订阅 git.changed——commit 只动 .git、fs watcher 看不见；repo 内 agent 标记盖过 git 干净态）→ 订阅 git.changed + repo 内 git 为全树唯一真相源（修订 ADR-0013 合并规则），e2e 追加 commit 后 `tree-git-*` 消失断言与列宽键盘调节断言 | renderer NEW `store/editor-tabs.ts`（纯模块）+ `workbench/GitDiffPane.tsx`；editorStore 抽 ensureDocument、新增 openDiff；contracts OpenTabsStateSchema +kind/staged（旧数据默认 file、旧构建剥离未知键）；i18n +40 词条（避开既有 `Stage:'阶段'` 错义键，git 用 `Stage file/Unstage file`）；README/官网无此界面描述，无需同步 |
| M13. 会话编排（agent 指挥 agent，ADR-0044）— 六个 terminal.* gateway 工具（send 过分类器动态定级）+ per-terminal token 身份 + unix socket 外部门（claude/codex 当司机）+ rail 家谱/编队区/导播位 UI；验收 ORCH-001..012 | VERIFIED (landed) | ORCH-001..012 VERIFIED；`npm run check` clean（Prettier + boundary 359 + TypeScript）；Vitest 99 files / 820 tests；`tests/e2e/m13-orchestration.spec.ts` 2/2（受管 Agent + 外部 Codex-shaped CLI）；`npm run test:security` clean（secret scan + 14 files / 139 tests + production build + Electron security E2E 2/2）；窄视口实机截图 `/tmp/charter-m13-orchestration-narrow.png` | Main：TerminalControlService、六个 Gateway 工具、per-terminal token unix socket、总开关与 IPC；Renderer：rail 家谱、编队墙、worker 横带、接管/交还、两档暂停、审批多入口、导播位、Replay ⌁；文档：manual + Codex AGENTS 片段 + README 中英说明。M13-01..06 均 VERIFIED (landed)，已纳入后续提交 |
| CTX-FOLLOW. 进入会话即切换工作上下文 — rail Files 树跟随所选会话的项目 (ADR-0046; user report 2026-07-22 "点击某个项目和项目下的 session 时 Files 也应该显示这个项目的文件") | DONE (landed) | `npm run check` clean (boundary 377); vitest 107 files / 856 tests 含 NEW `workspaceStore.follow.test.ts` 5 (followProject 切换+homePick 置位/同路径与空路径 no-op/失败还原 homePick+toast/openTaskRoom 跨项目触发 workspace.open/同项目不触发); e2e: `session-workbench.spec.ts` NEW "entering a session moves the working context to its project" (双 fixture 跨项目点会话 → rail-context 回落 A + Files 面板显示 A 的 README.md + 房间不丢) 4/4, 回归 project-files-restructure(+mock)/shell-v3/external-cli/room-context-feeding/pivot-shell/home-v2 25 用例通过 (shell-v3 一例并行负载 flake, 单跑 2.4s 过) | Renderer-only: `workspaceStore.followProject(path)` (homePick 抑制 IDE surface 跳转, 失败 toast+还原); `appStore.openTaskRoom` 收口所有任务会话入口经动态 import 跟随 (静态引用成环); SessionRail 裸终端行点击+键盘导航按终端 projectPath 跟随 (不下沉 openTerminalSession, TerminalPanel 动态 import 对 node 单测致命)。分组头折叠不切换; homePick 连点竞态与 startSession 同源未扩大 |
| M13-GUIDE. 编排工具自然语言选型加固（user report 2026-07-22：在 Claude Code 里说"开另一个 claude terminal 去审核"没开新终端 → 排查确认该案例不是 Charter，但暴露 M13 的真实盲区——mock E2E 全走 `[scenario:*]` 脚本，从未验证真实模型会从口语意图选中 `terminal.create`）| DONE (landed) | `npm run check` clean（boundary 367）；vitest tool-gateway + agent-runtime-pi 11 files / 110 tests 含新增 3（catalog 携带 create/list 引导；guidance 进入 Pi 工具定义 description；无 guidance 工具 description 字节不变）；mock `m13-orchestration.spec.ts` 3/3 回归；新 `m13-live-orchestration.spec.ts` 无凭证正确 skip | 两个修复：① promptGuidance 断线——gateway catalog 一直输出该字段但 `agent-runtime-pi` 组装 Pi ToolDefinition 时只用 description，send/write/command/verification 的引导从未到达模型；现拼进 description（唯一保证每个 provider 都送达的槽位）。② `terminal.create`/`terminal.list` 补意图映射引导（"用户要求另开 terminal/claude/codex 窗口做某事 → 调本工具而非自己代劳；create 前先 list 复用空闲 worker"）。新增 env 门控真模型 spec（real-gateway 约定，CHARTER_TEST_KEY/BASEURL/MODEL）：纯口语中文意图（无 scenario 标签）断言模型自主调 terminal.create（R2 卡出现）→ 编队出现 worker → `printf 'ORCH_LIVE_%s\n' OK` 防回显探针落屏 → REVIEW_READY；claude 当 worker 的变体留作手测 |
| M13-SURFACES. 外部 CLI 用户级指令面 + wrapper 链加固（ADR-0045；user report 2026-07-22：手敲 claude 会话说"创建一个 codex worker"没有 terminal_* 工具、模型退化为 Bash 探测 codex → 排查定位 MCP wrapper 注入链三重绕过：shell alias 优先于 PATH、zshrc PATH prepend 压过 binDir、claude 从 nvm 迁到 ~/.local/bin 后 stale wrapper 指向已删路径且启动日志 claude:false 不重写）| DONE (landed) | `npm run check` clean（boundary 376）；vitest 106 files / 851 tests 含新增 charter-terminal-surfaces 4（全新 home 未安装/双目标安装 byte 相等/过期检出并可刷新/单目标只读降级为 per-target error 不牵连另一侧）、terminal-control-integration +2（fallback 目录解析、CLI 消失清 stale wrapper）、terminal-control-manual +1（触发条件与门票自检落在 description/正文） | FanBox 路线落地：`skills.installCharterTerminal` v2 一键三目标（受管库不变 + `~/.claude/skills` + `~/.codex/skills`，tmp+rename 原子写、仅显式点击、byte 对比 upToDate 可更新），新 `skills.charterTerminalStatus`；SettingsView 行内显示各目标状态（checking/not installed/update available/up to date + 局部失败提示）；手册 description 补触发场景（中英口语示例）与 `CHARTER_CTL` 可用性门、正文开头门票自检、无 MCP 会话导向 charter-terminal 命令/curl 同一扇门；wrapper 加固：解析 PATH 后追加 ~/.local/bin、/opt/homebrew/bin、/usr/local/bin，CLI 找不到即删该 CLI 的 stale wrapper。Codex 侧确认 `~/.codex/skills` 原生支持（2025-12 起 SKILL.md 标准互通），AGENTS.md 片段降级为手动 copy-paste 备选 |
| M13-R0. 编排观察/内容注入免审（ADR-0044 修订 2026-07-22；user decision：实测后裁定去掉编排 R1 卡——read 和对 TUI 的 send 每步都要人批，一轮 observe→instruct 循环两次打断且无对应安全增益）| DONE (landed) | vitest tool-gateway 全绿（classifyTerminalSend TUI→R0、Ask catalog 收录 read 断言更新）；m13-orchestration e2e 3/3（受管流读卡断言改 0 张、外部司机审批循环删 read 行）；`npm run check` clean | terminal.read → R0（缓冲仅内存、账本只记元数据、屏幕本就用户可见；副作用=read 进 Ask catalog，只读会话可观察 worker）；terminal.send TUI 前台 → R0（内容归目标 agent 自己的审批流；投递仍受接管/两档暂停/预算/深度/自控约束，全文照进账本）；裸 shell R2 下限、控制键 R2、create R2、kill R4 不变；**写工具 R1 卡（Approve 模式问写入）明确不动**——用户诉求限定编排面 |
| M13-AUTO. Terminal 全命名空间免审（ADR-0044 修订 2026-07-25；真实多控制台日志显示 create/shell-send 在不可见权限卡内等待，被误判为 control-plane 写通道卡死） | VERIFIED | `npm run check` clean（Prettier + boundary 422 + TypeScript）；`npm test` 121 files / 988 tests；terminal/security targeted 5 files / 46 tests；`npm run test:security` clean（secret scan + 16 files / 148 tests + production build + Electron security E2E 2/2）；`m13-orchestration.spec.ts` 3/3（managed + authenticated external，零 terminal 权限卡） | terminal.* R0-R3 保留风险审计但不创建 permission request，standing deny 不阻塞；R4、自控、深度、预算、暂停/接管不变；kill 仅用户明确要求；compat CLI 增加 30s/长 wait 自适应超时；manual 强调 queued 未投递与常驻 TUI busy 语义 |
| M13-TURN. Worker 完成事件与低延迟控制面（ADR-0044 修订 2026-07-25） | VERIFIED (landed) | `npm run check` clean（Prettier + boundary 422 + TypeScript）；`npm test` 121 files / 991 tests；targeted terminal 4 files / 25 tests；production build；`m13-orchestration.spec.ts` 3/3（含 live resident Codex turn-completed→wait→完成状态）；`npm run test:security` 16 files / 149 tests + Electron security 2/2 | 新增 `terminal.wait(mode=turn)`：ExternalSessionService 的结构化 turn/result 与 observed settled 边缘直接唤醒等待者；单调 sequence 防 send/create→wait 竞态丢事件；常驻 TUI 可在 busy=true 时呈现 completed/failed；shell create 即时返回，350ms startup 窗内 send 有序排队而非阻塞 API |
| RAIL-COUNT. 项目卡会话计数口径修复 — Projects 面板计数不受 rail 分页影响，文案改 "N active · M outside" + tooltip 说明口径；externalSessionIndex 同一会话 id 挂多任务时优先非归档最新任务 (user report 2026-07-21 "5 sessions vs Tracked·6 是不是 bug" — 数字各自正确但口径混淆，排查出两个真实隐患) | DONE (landed) | `npm run check` clean (boundary 340); vitest 797/797 incl. NEW `rail-groups.test.ts` 4 (分组+History 末位/ended-answered 外部会话不计入/全量 vs 分页切片计数/recorded 含 History 不含裸终端) + NEW `external-session-index.test.ts` 5 (小写映射/坏行与空 id 跳过/非归档优先不论行序/同为非归档最新 updated_at 胜/全归档回退最新); e2e session-rail-polish + project-files-restructure 通过; CHARTER_SHOTS 临时 spec 截图确认卡片 "1 active" 与 title 断言后已删 | Renderer: 分组逻辑抽纯模块 `rail-groups.ts` (buildRailGroups/recordedTasksByProject)，卡片计数从未分页 allEntries 构建（原从 20 条分页切片派生，多页时偏小）；删除项目确认数改用 recorded 口径 (active+History)。Main: 索引折叠抽 `external-session-index.ts`：非归档 > 最新 updated_at，考古页 Tracked 行 Open 落到用户可操作的任务而非已归档副本。既有 `ui-shots-rail-icons.tmp.spec.ts` 门控 spec 发现已过时（第二任务今落 IDLE 与旧断言冲突），未在本次范围内修 |
| SKILL-INSIGHT-EXT. Skills 透视外部消费方 — Claude Code 单独用 skill 也计入使用量,按 Charter/Claude Code/Codex 分账 (ADR-0040; user decision 2026-07-21 "架构预留、首版只解析 Claude Code" + "UI 完整版:筛选+明细+分色火花线") | DONE (landed) | `npm run check` clean (boundary 337); vitest 788/788 incl. `skill-usage.test.ts` 11 (per-consumer 拆分+桶数一致+join 套件: slug/冲突限定/精确回退/插件名丢弃), `session-archaeology.test.ts` 11 (+skillEvents 时间戳/sidechain+无戳排除/服务级仅走 Claude 盘+consumer 标注+disabled 空), `channels.test.ts` 12 (+skills.usage v2 缺 byConsumer 拒绝), `skills-insight.test.ts` 10 (+projectUsage 投影/排序跟随/consumerBreakdown/sparkStacks/外部-only 不预选/cost 按 Charter 分母) | Main: usage 事件带 consumer,`aggregateSkillUsage` 单遍合计+切片(`byConsumer`);`TaskService.skillUsage`→`skillUsageEvents`(裸事件),聚合上移 `skills.usage` handler 与外部事件合并(try/catch 降级仅 Charter);`SessionArchaeologyService.skillUsageEvents()` 复用 mtime 缓存+并发合并,仅遍历 Claude 转录(`Skill` tool_use 带行级 timestamp;Codex 保留位恒零,格式未验证不猜写);`joinExternalSkillEvents` slug 化+同源优先(冲突限定名 `pdf@claude` 正确落账)+精确名回退,插件命名空间名丢弃。Contracts: `SKILL_CONSUMERS`/`SkillConsumerUsageSchema`,`SkillUsageDto` 顶层改合计语义+必填 `byConsumer`,channel v1→v2。Renderer: Via chips(All/Charter/Claude Code/Codex 带色点,Codex 置灰注明未支持)经纯函数 `projectUsage` 投影计数/火花线/排序;火花线周桶按消费方分色堆叠(charter #6ca1e8/claude #e0876a/codex #46b477);tooltip `consumerBreakdown` 非零消费方明细;`unused 45d` 徽章改全消费方合计为零;断舍离经济学仍按 Charter(合计零→预选,外部-only→候选不预选且理由注明外部用量,costly 分母改 charter uses)。首扫离关键路径(启动即 refreshUsage 预热) |
| NAV-SYNC. 左侧导航与主区域始终对应 — 导航组(workbench=sessions/inbox/files, projects)拥有主 surface，跨组切换快照+恢复，opener 反向摆正 rail (ADR-0042; user bug report 2026-07-21 "左侧菜单变化右侧不刷新，是一类问题") | DONE (landed) | vitest: NEW `appStore.nav.test.ts` 12 (分组/surface 派生/跨组清空+返程恢复/组内不触碰/opener 翻转+记忆/inbox 不被拽走); e2e: project-files-restructure + session-workbench(10, 含暴露 startSession 时序的用例) + m4-search-lsp-terminal 通过 | Renderer-only: appStore NEW `railGroupOf`/`mainSurfaceOf` 纯函数 + `savedSurfaces` 双槽 + `crossRailPatch`/`applySurface` 闭包; `setRailView` 收口跨组快照恢复(经原 opener 重放保不变量); openTaskRoom/openTerminalSession→sessions, openArchaeology→projects, setProjectTool/setSurface(workspace)/showSideBarView(search·scm)→files; SessionRail `startSession` 改"先切区后落意图"时序, 项目行点击删手动 setView('files')(store 已配对)。已知限制: 恢复不校验 task/terminal 存活(避免 appStore→taskStore 反向依赖) |
| CLIP-CARD. Clipboard image card — WeChat/Snipaste/⌘⇧⌃4-style clipboard-only captures pop the same quick card as file screenshots (ADR-0039; user decision 2026-07-21 "需要，直接帮我实现这个功能吧", 动因：排障发现用户截图全走剪贴板、从不落盘，目录 watcher 全盲) | DONE (landed) | `npm run check` clean (boundary 334); vitest 758/758 incl. NEW `clipboard-screenshot-watcher.test.ts` 9 (bare-image heuristic incl. text/html·uri-list rejects, baseline skip, fresh announce + PNG on disk + origin tag, fingerprint dedupe, same-second -2 suffix, 16-file retention, stale purge on start, oversize skip, dispose), `screenshot-watcher.test.ts` +1 (announce() dedupe + seen/recent registration); production build clean | Main: NEW `ClipboardScreenshotWatcher` — metadata-first poll (`availableFormats()` types query per tick; only a BARE image clipboard — `image/*` present, no text/plain·html·rtf·uri-list — triggers `readImage()` + bitmap sha1 fingerprint; text-bearing pasteboards are copied content, not captures), first poll baselines without announcing, idle backoff 1.2s×1.6→5s cap (reset on change/non-image), fresh fingerprints written atomically as `userData/clipboard-captures/Clipboard <date> at <time>.png` (-2 suffix, session-unique names — recycled names would hit the announce dedupe set and silently drop cards; 16-file FIFO, prior sessions purged on start), announced via NEW `ScreenshotWatcher.announce()` funnel (probe tail refactored through it) → same allowlist/recent/`screenshot.captured` pipeline, renderer read surface unchanged. Gate: darwin && !PI_IDE_E2E && `PI_IDE_CLIPBOARD_CAPTURE!=='0'` (clipboard not test-controllable → unit-injected read/thumbnail/now). Contracts: `ScreenshotCaptureSchema` +optional `origin:'file'\|'clipboard'` (event stays v1). Renderer: card header only — "Clipboard image" title + ⌘C glyph, aria-label follows; feed/annotate/save routes untouched. macOS 26 note: types queries skip the pasteboard alert; first content read may prompt once (Always Allow). am.1 (acceptance feedback): the no-active-session route fed Pi sessions a path-only text ref the model cannot see — NEW `charterRefs.ts` splits Home-charter refs: image-extension refs ride `task.start` fileRefs (kind 'image' + relPath → prompt image bytes via `resolveFileRefImages`, root-contained, ≤4/message, overflow + non-images stay textual), external CLI charters stay all-textual (`@ref`, own image tools); NEW `charterRefs.test.ts` 4 (schema-valid promotion, cap overflow back to text, non-image passthrough, block rendering), vitest 762/762. |
| ARCH-TIME. 考古页时间优先组织 — Today/Yesterday/Past 7 days/Earlier/Undated 日历日分桶，状态降级为 All/External/Tracked 筛选 chips (ADR-0041, 原 0039 让号给并行 CLIP-CARD; user decision 2026-07-21 "按你的建议组织修改代码"，动因：89 条全落 External 单组=分组无区分度，状态已在行内药丸+按钮编码两次) | DONE (landed) | vitest: `archaeologyStore.test.ts` +4 (本地日历日边界 00:05/23:55、6d/7d week-earlier 分界、空桶省略、未来/坏时间戳降级、filter 三态，本地时区构造跨 tz 确定); 全量 748 通过; `npm run check` clean | Renderer-only: `archaeologyStore` NEW 纯函数 `bucketSessionsByDay` (Math.round 吸收 DST, 桶内保持 host 新→旧序) + `filterSessions`/`ArchaeologyFilter`; `ArchaeologyView` 移除 Discovered/Tracked 分节 → 时间分桶节 + 顶部筛选 chips (含计数, `arch-filter-*` testids) + 筛选空态 `arch-filter-empty`; Directories 区块保持独立 (目录实体≠会话时间轴)。通道/schema 零变更。顺手修复: `database.test.ts` 两处迁移期望值未跟上 v8 (ADR-0037 遗留, [4,5,6,7]→[4,5,6,7,8], [7]→[7,8]) |
| SKILL-INSIGHT. Skills 透视 — 触发统计 + context 预算 + 独立 Settings 栏目 (ADR-0037; user decision 2026-07-20, mock `docs/design/skills-insight-mock.html` 方案 B approved, "从第一性原理" IA 讨论确认进 Settings 左 nav 而非主工作区侧栏) | DONE (landed) | vitest: NEW `skill-usage.test.ts` 6 (window/malformed/clock-skew/week buckets/join/ghost-drop), `skill-store.test.ts` +2 (expandCommandDetailed, preambleTokenEstimates ≈ preambleBlock length), `skills-insight.test.ts` 4 (totals/sorts/candidates/last-used), `channels.test.ts` +1 (skills.usage bounds); tsc clean | Main: migration v8 `skill_invocations` (explicit `/skill:` ledger; load_skill 已在 tool_calls) + idx_tool_calls_name_created; `TaskService.skillUsage()` 合并两账本(主加载才计数,bundled-reference 追加不重复计)→ 纯函数 `skill-usage.ts` 聚合;`SkillStore.preambleTokenEstimates()` 与 preambleBlock 同源估算(explicit-only/off = 0);新 channel `skills.usage` v1(handler 回调注入 TaskService,未就绪返回空聚合)。Renderer: Settings 新 section `skills`(SECTIONS Agent↔Models 之间,Agent 留 "Open Skills" 入口);`SkillsSettings.tsx` = 原 SkillsBlock 原样迁移 + 预算堆叠条(hover 联动)+ 45d 调用列(spark+末次)+ token 列 + `unused 45d` 徽章 + Catalog/Usage/Tokens/Cost 排序 + Review usage 断舍离(零调用预勾选,批量 Off,只建议不自动化);"/" picker 底部 `N enabled · ≈X tok/turn · Manage…`。e2e `skills.spec.ts` 导航 Agent→Skills |
| TERM-STAT. 终端路径 stat 验证边界 — 空格/CJK/折行路径可点击 (ADR-0033 am.1; 借鉴 FanBox, user decision 2026-07-20 "4 是必须要做的") | DONE (landed) | vitest: `terminal-file-links.test.ts` +11 (macOS 截屏名收复/CJK 空格路径/引号-中文标点-列缝边界/:line 剥离/批量上限/mid-token 锚点/折行拼接/宽字符 padding 剔除), `terminal-file-open.test.ts` +3 (verifyTokens 顺序/封禁塌缩 false/绝对路径归一), `channels.test.ts` +1; 全量 687 通过 | 新通道 `terminal.statTokens` v1 (≤24 tokens, 只读, openPath 同套 cwd 封禁逐 token 不抛错); 渲染端扩展名锚点左扩候选 (`detectWideCandidates`, 硬边界=引号/管道/glob/中文标点/双空格) + 最长存在者胜 (`resolveWideMatches`+`mergeFileLinks` 吞并窄命中) + 逻辑行拼接 (`readWrappedLine`, isWrapped 上限 8 行, 非末行 padding 剔除) — 解除 ADR-0033 已知折行限制; hover 触发+每逻辑行一次批量 IPC+64 行 LRU, 失败不缓存 |
| ARCH. 会话考古 — 发现并收编 Charter 之外的 CLI 会话 (ADR-0038; user decision 2026-07-20 "OK，你帮我实现吧", mock `docs/design/session-archaeology-mock.html`) | DONE (landed) | vitest: NEW `session-archaeology.test.ts` 9 (Claude/Codex 归纳、归属 cwd 最内层/files 投票/none、fs 扫描双存储/去重/uuid 过滤/日期窗/disabled), NEW `archaeologyStore.test.ts` 3 (scope 前缀不误伤兄弟目录/未知目录分组/新鲜度), `channels.test.ts` +2 (adopt uuid 正则拒注入/archaeology context 无原始路径), `external-session.test.ts` 原样通过 (resume 重构无回归); tsc+prettier+boundaries 通过 | Main: NEW `session-archaeology.ts` (只读扫描 ~/.claude/projects + ~/.codex/sessions 近30天, (path,mtime,size) 缓存, >50MB 跳过, E2E 仅 PI_IDE_ARCHAEOLOGY_HOME); `TaskService.externalSessionIndex()` 去重; `ExternalSessionService.adopt()` + `startResumedSession` (自 resume() 提取共享尾部, 12s 未检测→任务退役); NEW channels `archaeology.scan/adopt` + terminal.create context kind `archaeology` (host 从发现缓存解析 cwd, 渲染器零原始路径); Renderer: NEW `archaeologyStore` (60s 新鲜度+单飞 adopt) / `ArchaeologyView` (HomeShell 新分支, scope=项目/目录/全机, External/Tracked 分组, 空/加载/禁用态) / Projects 面板 "N outside" 小字+每行 🕘 入口+底部 Agent activity 组。后续项: E2E 假 home 整链、收编房间只读历史导入、View 只读回放 |
| SHOT-CARD. Screenshot quick card — a fresh OS screenshot pops a bottom-right card: feed the active agent (Pi composer chip OR Claude Code/Codex input line), annotate first, or archive to project assets (ADR-0036; user decision 2026-07-20, mock `docs/design/screenshot-quickcard-mock.html` approved, "不仅是 Composer，Claude Code/Codex 也要能收到图" explicit) | DONE (landed) | `npm run check` clean (boundary 320); vitest 715/715 incl. NEW: `screenshot-watcher.test.ts` 7 (settle probe, dedupe, dotfile/non-image/probe-rejected skips, dispose, growing file), `screenshot-handlers.test.ts` 9 (seen-set gate incl. /etc probe, -2/-3 suffixes, PNG magic, name sanitization, original untouched), `channels.test.ts` +2, `screenshotStore.test.ts` 4 (stack/pop/clear/revision), `screenshotFeed.test.ts` 7 (pi/external/ended/cross-project/none routing, annotatedName); production build clean | Main: `ScreenshotWatcher` (dir via `defaults read com.apple.screencapture location` → ~/Desktop fallback; `fs.watch` + size-settle probe; screenshot-ness = locale filename patterns OR `mdls kMDItemIsScreenCapture` with one lagged retry; only files born after start; `PI_IDE_SCREENSHOT_DIR` override = deterministic always-true probe, E2E without override = off, non-darwin override-only) → `screenshot.captured` event (nativeImage JPEG thumb ≤360w). Renderer reach is ONLY watcher-seen paths: `screenshot.read` (annotator base), `screenshot.saveToAssets` (path or PNG-magic bytes → `assets/screenshots/`, never overwrites, atomic, `resolveInsideRoot`), `screenshot.recent`. Card (`ScreenshotQuickCard`, mounted beside toasts): no focus steal, 8s auto-dismiss (hover pauses; timeout/✕ = zero side effects, clears stack; acting pops next; "+N more" badge), card-scoped Enter/E/S/Esc guarded against input/textarea/xterm keyboard owners. Primary action routes like `addCodeContext`: Pi → `task.attachments.import`(path) → fileRef chip + focusComposer; external ACTIVE same-project → saveToAssets + `external.injectContext` `@relPath ` (ADR-0030 contract, unsent); else → saveToAssets + `addPendingRefs` (rides next charter). Annotator extracted from ImageView into shared `Annotator.tsx` (actions parameterized; ImageView behavior unchanged) — quick-card annotate reads via `screenshot.read`, exports PNG bytes down the same three routes. |
| FIX-0720C. Cold-start `models.list failed` error on every launch (user report: "启动时候报错 … 没配置 base url/key 不应算报错") | DONE (landed) | `npm run check` clean; vitest 202/202 (desktop-main + agent-worker + runtime-mock); live restart shows a clean startup log — no error AND no warn (previously errored on every cold start before `agent worker ready`) | Root cause was NOT missing credentials (`listModels` works unconfigured — registry models with `configured:false`): `AgentHost.ensure()` had a cold-start race — while one caller's spawn+init was in flight, a second caller saw `worker && !initialized`, returned early and posted `listModels` into a worker whose runtime was still `null` → TypeError → generic "An unexpected error occurred." logged at error level. Three layers: ① `ensure()` keeps a `spawnPromise` and concurrent callers JOIN the in-flight spawn+init (loop re-checks state; kind-switch semantics preserved); ② worker guards `listModels`/`validateCredential` with a typed retryable `AG_NOT_READY` refusal instead of a null-deref; ③ `models.list` handler logs retryable (self-healing: worker starting/restarting) failures at `warn`, keeps `error` for real ones — response already degrades to empty catalog + renderer refetches on `agent.workerStatus`. |
| FIX-BATCH-0720B. Three user bug reports (ADR-0034): ①rollback leaves stale Diff panel ②narrow-room composer clipping + phantom horizontal scrollbar ③no way to delete a project | DONE (landed) | `npm run check` clean except two PRE-EXISTING type errors in the user's in-progress `RecapDepth.tsx` (storyFacts/inline — untouched); vitest 325/325 across ipc-contracts + persistence + desktop-main + tests/unit incl. NEW: migration v7 repair (fix/no-touch matrix), StateService.removeWorkspace (cascade both-direction conv-refs / running+external-active refusal / missing path), activity projection filesReset (task.rolledBack resets, turn.rolledBack stays additive) | Root cause ① was TWO bugs: a legacy `external_json.status='interrupted'` row poisoned EVERY `task.list` response (route-level response validation fails the whole array → `refreshTasks` dead since; dev log `ipc response schema violation`) — fixed by migration v7 + `rowToDto` read-side normalization; and the renderer never reset projections — `taskStore.rollbackTask` now clears+refetches `changeSet`, `ActivityItem.filesReset` (set only by `task.rolledBack`) makes the shared pure projection restart `filesTouched` (live fold == replay). ② Session Canvas breakpoints moved from window `@media` to `@container task-room` (`.tr-root` is the size container; 880px stack / 700px squeeze) — a media query cannot see what rails/sidebars leave the room; hardening: `.rt-scroll{overflow-x:hidden}`, `.tr-ccard{min-width:0}`; Home keeps an equivalent `@media 920px` for the shared `hm-*` composer rules; overlays verified outside the container (fixed-position semantics unchanged). ③ New `workspace.remove` v1 channel → `StateService.removeWorkspace` single-transaction cascade over all task/workspace-scoped tables (disk files NEVER touched; blobs stay — no delete path exists by design; refused while any session runs or an external CLI session is active; open project closed first); rail Projects rows get a hover trash `ArmedIconButton` (arm→confirm, extra `window.confirm` when recorded sessions exist); `ArmedIconButton` now honors its `icon` prop (was hardcoded `archive`). |
| TERM-LINKS. Terminal file links — ⌘+click a file token in any terminal (Claude Code / Codex / shell) to open html/svg/pdf in the system browser, other files in the editor at `:line` (ADR-0033; user mockup approval `docs/design/terminal-file-link-mock.html`, chose ⌘+click + system default browser) | DONE (landed) | `npm run check` clean; unit 641/641 incl. 4 new `channels.test.ts` openPath cases, 6 `terminal-file-open.test.ts` (browser/editor split, cwd containment), 11 `terminal-file-links.test.ts` (bare tokens, CJK cell mapping, URL-glue and version-number non-matches) | New `terminal.openPath` v1 channel: main resolves token against THAT terminal's launch cwd via `cwdRelativeToken` + `resolveInsideRoot` (WS-010 rules), stat-must-be-file, `TERMINAL_EXTERNAL_OPEN_EXTENSIONS` (shared const) → `shell.openExternal(file URL)`, else `{action:editor, workspacePath}` for `doc.open`/`revealPosition`. Renderer: xterm `linkHandler` (OSC 8 file:// + https via `app.openExternal`) + regex `registerLinkProvider` fallback with string↔cell mapping (`readBufferLine`); modifier-required activation with teaching toast, hover hint `.terminal-link-hint`. Handler in `ipc/terminal-open-handlers.ts` (m4-handlers stays electron-free). Also fixes dead web links (WebLinksAddon default `window.open` was always denied by §12.3). Known limits: launch-cwd only (no `cd` tracking), no wrapped-line tokens, Windows file:// pathname deferred to M12. |
| TREE-DIFFSTAT. Explorer diffstat decorations — untracked/added merge into one green **A**, modified/renamed rows show **+N −M** line counts before the letter (ADR-0013 revision 2026-07-20; user report "全是 U 分不清新增/修改", mock `docs/design/filetree-diffstat-mock.html` 方案 A approved) | DONE (landed) | `npm run check` clean; unit 630/630 incl. 4 new `git.test.ts` numstat cases (counts vs HEAD, rename→new path, binary flag, no-HEAD repo → empty); e2e `p4-review-v2.spec.ts` 3/3 (A letter, `tree-diff-*` +2−0, new-file has no counts, non-git agent-marks fallback); screenshot review vs mock passed | `GitService.numstat()` (`diff --numstat -z HEAD`, NUL-parsed incl. rename records); `git.status` contract v2 adds `stats` (binary files excluded; numstat failure degrades to `stats: []` — decorations never break status); `gitStatusStore` drops `U` from `GitMark`, maps untracked→`A`, adds `statByPath`; ProjectTree renders green `+N`/red `−M` (tabular-nums, mono 10px) before M/R letters only — A/C stay letter-only, tabs unchanged. Non-git projects keep agent-mark letters without counts. |
| EXT-SINGLE-INPUT. External session single input surface — the bottom product composer is deleted; the CLI's own input line is the room's only conversation entry; drags and code selections inject into it unsent (ADR-0030, user decision 2026-07-20, mockup `docs/design/external-terminal-single-input.html` approved, selection variant B chosen) | DONE (landed) | `npm run check` clean; unit `external-session.test.ts` (externalInjectText file/folder/selection/no-CR) + `code-context.test.ts` (ExternalInjectRefSchema escape rejection) green; e2e `code-context.spec.ts` 3/3 (frozen snapshot reaches claude/codex PTY via `external.injectContext`), `claude-session-identity.spec.ts` 4/4 (observed-reply presence now driven by typing into the room terminal) | `ExternalContextComposer` + `external.message` + `sendMessage` removed (launch-intent first-prompt delivery untouched). New versioned channel `external.injectContext` → `ExternalSessionService.injectContext`: bracketed paste, deliberately no Enter; file refs `@path ` (folders trailing `/`), selections as the frozen `formatPromptWithCodeContext` block (payload via pure `externalInjectText`). Terminal column accepts tree/OS drops (live: blue drop veil "nothing is sent until you press Enter"; ended: interception card with Resume via `externalStore.resumeTask`; outside-project items toasted). `addCodeContext` routes external tasks to injection (managed chip path unchanged). Ledger: `external.contextInjected` event (cli/kind/path/range/hash) with an activity mapping; session naming now comes from the first committed typed line (placeholder-guarded). Follow-up fix (user field report, same day): `isAccountablePath` now also ignores third-party CLI atomic-write temp files (`name.tmp.<pid>.<hex>`, end-anchored regex) — Claude Code's write-then-rename temp path was accounted as a phantom second changed file (live-board tile + diff badge); unit-tested incl. lookalike names that must survive. |
| SINGLE-TREE. One project tree — rail Files pane absorbs the Explorer; the Editor surface drops its Files tool column (ADR-0029, user decision 2026-07-20, merge option chosen from screenshot review, mockup waived) | DONE (landed) | `npm run check` clean; unit 607/607; affected e2e 46/46 green in two batches (project-files-restructure rewritten to the new invariants — no Files tab, rail tree with inline New File create, Search/Changes toggle-to-collapse, 900px narrow rail keeps the Files panel; m3-editor 5/5 incl. context-menu walk against the rail tree; room-context-feeding 2/2 on the new `tree-item-*`/`tree-add-*` ids; m4/m5/m10/p3/p4/pivot-shell/shell-v3/shell-v4/session-canvas/session-rail-polish/home-dragref regressions green) | New `ProjectTree.tsx` = ExplorerView engine (virtualized rows, context menu New File/Folder + Open in Browser + Rename + Trash + Copy paths, inline inputs, git marks/dirty dots, glow) + HomeProjectTree behaviors (drag via dragRefs, hover “+” quick-add, room-of-this-project click = Peek per PIVOT-027r, ⌘/⌥-click Editor jump). Mounted only in `SessionFilesPane` (header gains New File/Refresh/ignored-toggle; no-workspace empty state gains Open Folder) and `SessionTerminalView` (replaces its embedded ExplorerView). `ExplorerView.tsx`/`HomeProjectTree.tsx`/`HomeSidebar.tsx` deleted (`needsAttention` → labels.ts). `ProjectTool` `'files'`→`'editor'` (plain editor; tabs Search/Changes only, active-tab click collapses; context aside stays mounted aria-hidden+inert — the 2-col grid would swallow the stage otherwise). `railView` lifted to appStore (same sessionStorage key): Projects-panel project click = editor surface + rail Files; ⌘⇧E flips the rail only; no other entry hijacks the rail tab. Editor empty group gains "Browse project files". |
| MEMORY. Project memory — review-as-learning rules, one source three projections (ADR-0028; retention-research proposal ①, mock views Ⓐ/Ⓑ/Ⓒ user-approved 2026-07-19) | DONE (landed) | Unit 591/591 incl. 47 new (rules-file lenient parse/round-trip 9; managed-block idempotent upsert + drift primitives + convention scan 8; similarity calibration zh-bigram/en-word 5; external-memory discovery/munge/containment/binary/backup-delete/mtime-conflict 8; MemoryService capture→merge→hit→approve→inject→sync→drift→import→promote walk 17) + privacy clear-history fixture extended with memory rows (FK regression) + persistence upgrade test now asserts [4,5] and the four memory tables; `tests/e2e/memory.spec.ts` 3/3 (request-fix → inline distill card → rule ON DISK in `.charter/rules.md` → "injected into N tasks" after the very next run; AGENTS.md sync enable → managed block on disk → hand edit inside block → drift with file untouched → import rescues the edit into candidates and rewrites; fake home PI_IDE_MEMORY_HOME: discover CLAUDE.md/MEMORY.md/note → view → promote(copy) → approve → rule on disk with original untouched → backup-first delete into userData/memory/trash); adjacent regressions p4-review-v2 + m8-review + m6-agent-ask + skills + session-workbench 16/16; `npm run check` clean (boundary 305); production build clean | One rules source per project (`.charter/rules.md`: checkbox list + `charter:id` comments; hand-edit safe — unknown lines round-trip byte-identically, hand rules get ids on next write; git-shareable) with machine-local DB halves (migration v5: candidates, rule stats, injections, sync state). Capture rides the existing gates: request-fix steers (review-origin code refs) and plan request_changes become pending candidates — deterministic similarity (ASCII words + CJK bigrams, 0.35 threshold calibrated in tests) merges repeats (×N), and a correction matching an enabled rule counts a "slipped again" hit instead of a duplicate. Distill card renders the task's candidates inline above the composer (approve editable / dismiss / unhandled stays queued — never lost, never auto-applied); `settings.memory.captureEnabled` gates capture. Enabled rules ride every managed run as a `<project_rules>` preamble block and — because the preamble is per-session, not per-run — are re-attached on every later run of a reused session AND every mid-run reply (the exact skills-catalog freshness split); injections recorded per rule×task drive the honest "injected into N tasks" counter, and approving a task-sourced candidate writes a `memory.distilled` timeline receipt into the source task (rt-note, replay-traceable provenance). Managed-block projections per project, default OFF: CLAUDE.md = one `@.charter/rules.md` import line; AGENTS.md = rendered list (no import semantics); writes atomic and only between `charter:rules` markers; drift (hash vs last-written, incl. first-enable over a foreign block) is never overwritten silently — import/overwrite/stop three-way, plus reverse-import of hand-written bullets outside the block. Private memory managed-not-merged: read-only discovery over known roots (`~/.claude` global + munged project auto-memory via the ADR-0017 helper, `~/.codex/AGENTS.md`), opaque ids only (caller paths never reach the fs), logical+realpath containment, explicit-action writes with mtime-conflict rejection, backup-first delete, one-way promote; transcripts out of scope; Codex shows an honest "no auto-memory in this version" empty state. Surfaces: rail's fifth icon ◈ Memory → overlay (rules / sync & distribution / claude / codex / charter sections), ⌘K Memory group + Open Memory action, Settings → Agent capture toggle + shortcut. Honest deltas vs mock: the card offers after every correction (single quiet card, similar corrections merged ×N) instead of the mock's "2nd similar" threshold; stats show injected/slipped-again real counters instead of "挡下 N 次返工" (a prevented-rework claim needs proposal ② as checker); rules read from the project main root, not per-worktree (rules are configuration, not code under test). |
| EXPLORER-OPEN-IN-BROWSER. Explorer context menu "Open in Browser" for HTML files (user request 2026-07-19) | DONE (landed) | `npm run check` clean; ipc-contracts channel tests 4/4; m3-editor e2e 5/5 incl. NEW context-menu walk (html file shows the item, click closes menu with no error toast, README.md does not show it); screenshot pair reviewed (/tmp/ui-shots/open-in-browser-\*.png via `ui-shots-open-in-browser.tmp.spec.ts`, CHARTER_SHOTS-gated) | New channel `fs.openInBrowser` v1 (`{path}` → `{opened}`) — preload bridge picks it up from the registry. Main-process `WorkspaceHost.openInBrowser` re-checks policy (renderer only decides menu visibility): `resolveInsideRoot` + `.html/.htm` allowlist, `shell.openPath` (OS default browser), `PI_IDE_E2E` skips the real open like `app.revealPath`. `app.openExternal` stays https-only — file URLs never widen that allowlist. Menu item sits between New Folder… and Rename…, files only. |
| EXTERNAL-LAUNCH-INTENTS. Composer→CLI first-prompt delivery, pre-assigned claude session ids, user-named sessions, honest "Ended" state (ADR-0017 amendment 2026-07-19) | DONE (landed) | Unit 537/537 incl. 20 new/updated (ExternalLaunchIntents one-shot/TTL/mismatch, externalTitleFromPrompt, terminalLaunchCommand UUID guard, presentedMeta external-awareness); external-cli e2e 6/6 incl. NEW composer-launch walk (rpc terminal.create with initialPrompt → probe proves `--session-id <uuid>` argv, canonical-mode line delivery proves paste+separate-Enter, task titled "hi from composer", sessionId == launch uuid, row lands REVIEW_READY with `Ended`/`Session ended · no file changes`, resume argv `--resume <same uuid>`); regressions session-rail-polish/terminal-remount/m4-terminal/shell-v4 15/15; boundary 285 + tsc + build clean | Field report (user test drive): composer first message sat unsent in Claude's input (renderer blind paste+CR at 350ms raced TUI startup; CR inside the paste chunk reads as pasted text), sessions listed as `Session <id>`, resume degraded to `claude --continue` → "No conversation found". Fixes: `terminal.create` v3 `initialPrompt`; one-shot TTL launch-intent registry (m4 handler → ExternalSessionService on the detection edge); claude launches `--session-id <uuid>` (strict-UUID PTY guard) so resume targets the exact conversation from minute one (and `--resume` keeps the SAME id — 2026-07-16 fork assumption corrected); host delivers the prompt after first-paint settle (600ms quiet / 8s deadline) as bracketed paste + separate `\r` (sendMessage unified on the same rule) and records the `user.message` ledger event; external tasks titled by the first user message (creation or first in-room send; `setExternalTitle` broadcasts a row refresh); zero-change exits present "Session ended"/`Ended` (rail badge+ticker, room dock, timeline milestone zh/en, completion notice) instead of the Pi "Answered" veneer. **Follow-up same day (amendment 2)**: resume still failed in the field — root cause proven by controlled PTY experiments: the dev app was launched from inside a Claude Code session, Charter PTYs inherited `CLAUDECODE`/`CLAUDE_CODE_*` markers, and a nested interactive claude writes NO transcript at all (print mode does — why the earlier `-p` verification passed). Fix: `sanitizedTerminalEnv()` strips ambient agent-session markers from every user PTY (allowlist `CLAUDE_CONFIG_DIR`; `ANTHROPIC_*` untouched; login shell restores user-profile exports) — verified live-transcript-under-preassigned-uuid on the user's machine. Reply notices now name the message they answer (`TypedLineTracker` best-effort PTY input reconstruction, product writes suppressed, `external.turn`/`external.activitySettled` v2 `lastUserMessage`, renderer falls back to session title) and the observed settle window dropped 1.8s→1s (spinner animates while working, so quiet == done). Evidence: 544/544 unit (+7: sanitizedTerminalEnv 2, TypedLineTracker 5), external-cli e2e 6/6 re-run, boundary 289 + tsc + build clean. |
| SESSION-CANVAS. One Session object + contextual tool canvas (PIVOT-037, ADE object-model absorption, 2026-07-17) | DONE (landed) | `npm run check`; unit 470/470; production build; complete Electron suite 106 passed / 13 manual-or-environment-gated skipped; latest Session Canvas/File Peek/Review/Editor/worktree/live-presence regressions 16/16 plus focused Diff 1/1 and heartbeat 1/1; 1440×900 and 900×900 real-Electron captures; user Diff reference and implementation normalized into one comparison image; all external CLI scenarios green; zero renderer console/page errors; `design-qa.md` final result passed | Runtime second shell removed: Workbench always mounts one Session Rail plus HomeShell; no global Activity Bar, legacy Sidebar, or duplicate Agent Panel is mounted. One Composer Agent Picker dispatches Charter/Pi, Claude or Codex. A Session-owned Summary/File/Diff/Preview/Terminal/Review canvas balances at 56/44, expands to 38/62 and reorders below the conversation at narrow widths. Review defaults to an evidence ledger with one Action Dock. The supplied Diff reference is reproduced with File/Diff/Preview/Terminal/Review tabs, change totals and selectable file ledger, line-numbered inline hunks with Monaco syntax color, compact verification evidence, copy/advanced-review entry, and one Request changes/Rollback/Approve changes Dock; opening Diff focuses the tool canvas while File Peek remains the independent File state. Zero-change answers stay on Summary; rollback retires the obsolete Review state. The historical three-layer execution presence is restored inside this single shell: persistent Session Rail ticker, one Launcher Live Board per running Session, and the Room's live timeline row + event-driven file heat tiles + activity strip. All layers project the same recorded task/change events; true writes drive hot/warm/cool decay, rhythm bars, the writing beacon, and direct live-file → Diff entry. Before a Session exists, Project Tools retain arbitrary editing, split/conflict handling, Search/Replace, Git Changes, Problems, Quick Open, and terminal entry inside the same shell. The mature Terminal Parallel manager preserves real PTYs and external session accounting. Legacy surface/layout command ids remain compatibility aliases into these contextual states. |
| EXTERNAL-SESSION-IDS. Per-session conversation ids + terminal ownership (ADR-0017 amendment 2026-07-16) | DONE (landed) | Unit 470/470 incl. 9 new (cli-session-locator: claude munge rule verified against real installs, window filter newest-wins, missing-dir/pre-window null, codex date-tree + rollout UUID extraction, unknown CLI; externalResumeCommand: id-targeted commands, degrade without id, non-UUID never reaches the PTY); `npm run check` + build clean; external-cli e2e re-run — identical failure set to clean HEAD (the 4 dock-surface failures are pre-existing; no new failures), lifecycle-adjacent tests green | Field report from the user's acceptance walk: (1) three sequential Claude sessions in one terminal → three tasks, every room re-parented the same live PTY (rows "didn't switch"); (2) resume wrote `claude --continue`, which continues the directory's MOST RECENT conversation, not this task's. Fixes: ExternalTerminalColumn mounts the terminal only for its owner (live session, or last session until a newer one takes the PTY — `taskByTerminal` projection); superseded rooms show their own record with an honest note. `TaskExternalSchema.sessionId`: captured live from structured streams (claude `system/init`/`result` `session_id`), discovered at session end for interactive TUIs from the CLI transcript stores (claude `~/.claude/projects/<munged-cwd>/<uuid>.jsonl`, codex `~/.codex/sessions/YYYY/MM/DD/rollout-…-<uuid>.jsonl`) bounded by the session lifetime, and backfilled at startup for recent pre-amendment tasks. Resume: `claude --resume <id>` / `codex resume <id>`, strict-UUID guard before PTY write, most-recent-flag fallback. Honest limits: discovery is best-effort mtime matching; fake e2e agents write no transcripts (fallback path keeps those specs' expectations); custom CLIs unchanged |
| RAIL-D. Session Rail direction D — grouped activity rail (ADR-0023, PIVOT-038/039 + 011r/013r2/027r2) | DONE (landed) | Unit 461/461; `npm run check` clean; `session-workbench.spec.ts` 2/2 incl. NEW direction-D walk (project group header with "1 need you" badge + count → amber Needs-you row → Inbox panel → row routes to the room → collapse hides rows but keeps the badge → expand restores → working-context row routes to the Projects panel with the active recent + tree); regressions on the final build: shell-v4 3/3, home-dragref 3/3, skills 2/2, pivot-shell 3/3, shell-v3 5/5, p3-full-mode 2/2, home-v2 2/3; screenshot walk 3 shots reviewed (sessions grouped/inbox/projects — /tmp/ui-shots/raild-\*.png). Pre-existing failures reproduced on clean HEAD via checkout-HEAD A/B: home-v2 PIVOT-012/015 (advanced criteria missing from the charter) and external-cli 4 (editor terminal dock: session bar, open-room button, resume echo) — all predate this work, unrelated to the rail | Mockup-first (docs/design/sidebar-vnext-gallery.html, four directions; user picked D = C's activity-bar shell × B's project grouping). SessionRail becomes a 46px activity bar (Sessions / Inbox badged / Projects / Search·⌘K + Editor / Settings) driving one context panel. Sessions panel: project groups (display-name keyed), collapsible with tree indent, per-group needs badge + count, rows drop the project name; settled sessions (ACCEPTED/ROLLED_BACK/CANCELLED) fold into a default-collapsed cross-project History group — attention states never move there, and the open room's group auto-expands (accept keeps the selected row visible; localStorage-persisted collapse). Amber Needs-you row ⇄ Inbox destination are one queue (PIVOT-013r2: Inbox opens the triage panel; row → room). Projects panel hosts recents/tree/Open folder/New project (PIVOT-011r; switching projects auto-returns to sessions). Resident working-context footer row → Projects panel. Selection = B's inset accent bar; ⌘1-9/⌘[⌘] order mirrors the visual order (History last). Restored contracts the recent shell rework had silently broken: `home-settings` (home-v2/shell-v4/skills) and `home-task-ticker-*` (shell-v4 heartbeat). Spec updates are navigation-only; no assertion weakened. Honest limits: grouping by project display name (basename collisions merge visually); History bounded by the rail's 20-task window; collapse state is renderer localStorage, not layout.save |
| PREVIEW-ROOM. Live preview as the Room's window — persistent rail, element pick, console self-heal, Full-mode reach (ADR-0022 am.2) | DONE (landed) | Unit 459/459 incl. 9 new console-policy (dedupe, write-attribution window, one-send-per-burst + 5/run cap, format, write-tool detection); `tests/e2e/room-preview.spec.ts` 2/2 (Full-auto ACCEPTED task → badge lights only after a server starts, layout unmoved → open rail → element Pick injects picker, click `#hint` inside the iframe → composer chip with the selector → closed task seeds a follow-up whose first run acks `[received 1 image attachment]`; running task → console `console.error` surfaces on the ⚠ chip → manual send lands `[Preview console]` steer → Draw region → chip → same-run fix acks the image); `preview-gate.spec.ts` 5/5 re-green after the gate adopted the shared Draw mode; p4/shell-v4/p3-full-mode/room-reviewbar regressions green; visual walk `ui-shots-room-preview.spec.ts` 5 shots light+dark reviewed (`/tmp/ui-shots/rp-*.png` — Full-auto task with live rail + badge, pick-armed hint, selector chip in composer); `npm run check` + production build clean | Reframes ADR-0022: the preview is the Room's window, not the gate's evidence — driven by a user who couldn't find it twice (buried in the gate; Full mode never opens a gate). One `LivePreview` component, two variants: the Room's resizable `RoomPreviewRail` (any task state) and the gate Preview tab. Detection lights a header `PreviewBadge`; the rail opens on click and shares the peek's side slot. Feedback is point-and-say: Pick (S) injects a self-cleaning element picker via `webFrameMain.executeJavaScript` into ONLY the task's loopback frame (poll-for-commit; falls back to the zero-injection Draw marquee on any failure), posting `{selector, rect, text}` to a port-origin-filtered `message` listener; Draw (R) is the marquee. In the rail the selection becomes a composer attachment chip (`draftStore.previewRefs`) — one input, one conversation, same steer loop; after a task closes it seeds a follow-up (`task.start` v2 `preview`). Console self-heal: zero-injection relay (`preview.console` from the window's own `console-message`, loopback-filtered) + pure renderer policy (`preview-console.ts`): `auto` forwards errors only while running AND within 15s of the agent's own write, one per burst, capped 5/run; `manual` = ⚠ chip + one-click; `off` = count. Setting `preview.consoleToAgent`. Honest limits: auto-forward's DECISION is unit-tested (9 cases) and capture+manual-send is E2E-tested; the live auto-timer is their composition (staging "running + write + error within 15s" against the fast mock is racy, not covered E2E). Picker injection is the first code Charter runs inside a preview page — fenced to the task's own port, posts-only, self-cleaning. |
| PREVIEW-GATE. Visible acceptance — Preview/Checks tabs in the gate, marquee feedback, PR draft (ADR-0022; cmux 移植研究 ④) | DONE (committed b37d71d/25e9f6e) | Unit 449/449 incl. 17 new (lsof listener/cwd parsing, loopback-only binds, worktree-vs-main attribution incl. sibling-prefix footgun, webish heuristic, PR branch naming, verification matrix superseded/stale/unverified lines, command quoting + no-`&&` GIT-007 sanity, CSP directive pin); `tests/e2e/preview-gate.spec.ts` 4/4 (a dev server cwd'd in the task worktree is listed while a main-tree server on another port is NOT; marquee → note → send → compact bubble + screenshot thumbnail on the timeline → fresh run acknowledges `[received 1 image attachment]` through the real worker protocol → plan gate → REVIEW_READY; accept → PR draft card with Goal/Changes/Verification/GIT-007 body → dismiss keeps `tl-pr-draft`, and `git log`/`branch --list charter/pr/*`/`status` prove no commit, no branch, no push; verify-fail-fix task shows both runs in Checks, old one superseded, latest passed; non-web project never grows a Preview tab while Checks stays); full E2E regression 94 passed — the 2 failures (E2E-002 editor restore, E2E-005 TS intelligence) reproduce on clean HEAD via stash, pre-existing; visual walk `ui-shots-preview.spec.ts` 11 screenshots light+dark reviewed (`/tmp/ui-shots/pg-*.png`); `npm run check` + production build clean | The gate's three tabs are three projections of one `task → main` question: code (Changes, untouched testids), checks (existing `task.verificationRuns`, VER-005/007/008 semantics presented not re-implemented), pixels (Preview). Port detection is read-only `lsof` scoped by process cwd inside the task's context root — a worktree task can never show a main-tree server and vice versa; no auto-start, honest empty state names the boundary. CSP `frame-src` widened exactly `'none'` → `http://localhost:* http://127.0.0.1:*` (csp.ts extracted + unit-pinned; iframe sandboxed `allow-scripts allow-same-origin allow-forms`; will-navigate/webview/window.open/permission hardening unchanged). Marquee feedback rides the SAME request-fix steer loop (no second comment system): screenshot via compositor `capturePage`, selection burned in renderer-side, PNG persisted under workspace attachments (never in any tree), timeline `user.message` carries `{note, rect, pageUrl, thumbDataUrl}` and renders note-first with the full agent message folded; runtime contract `steer/followUp/StartRunInput` gained optional `images` end-to-end (Pi `session.prompt({images})` native — no SDK patch; mock acks deterministically). PR draft generated at accept (manual + full-mode) from the evidence ledger — goal/acceptance, merged file list ±, verification matrix, replay receipt sha256 — recorded as `task.prDraft` event + returned to the accept caller; card + timeline entry are copy-out only (`git switch/add/commit/push` + `gh pr create --draft --body-file`, one command per line, never chained, never executed — GIT-007 byte-true; `charter/pr/` prefix avoids the audit-branch collision since merge-back never commits to it). Security tightening shipped with it (the hole became load-bearing once PR drafting exists): forge CLIs `gh`/`glab`/`hub` were unclassified (R2/R3 → full-mode auto-allow could publish with stored auth); the command classifier now sends forge mutations — and unknown/`api` subcommands, fail-closed — to R4 like `git push`, keeping explicit reads (`pr view/list/checks`, `run view`, `auth status`) at R3; covered by 2 new classifier tests and re-green m7-permissions + p3-full-mode E2E. Honest limits: `task.previewOpenExternal` is loopback+detected-port only; non-web heuristic = committed package.json dev/start/serve/preview script (worktrees see HEAD, so an landed script won't mark the isolated tree webish); cloud preview URLs, embedded DevTools and CI readback deliberately out (ADR-0022). |
| LEGIBLE-TERMINAL. Blocks / marker rail / sourced progress / command notifications (ADR-0021, TERM-007..011) | DONE | Unit 432/432 incl. block state machine 14 (OSC 133 lifecycle, lost-D honesty, multiline commands, rerun links, scrollback trimming, ⌘↑/⌘↓ stepping, three progress sources with 20s expiry, turn blocks), shell-integration spawn plans 11 (zsh ZDOTDIR shim incl. user-ZDOTDIR preservation, bash --init-file, fish vendor_conf.d, full degradation matrix), command notifications 7 (unfocused-only, min-duration, one-edge-one-banner, click→reveal); `tests/e2e/terminal-blocks.spec.ts` 3/3 (rail dots + ⌘↑ jump + block actions, sourced 42% on status bar/row ring + finish bell when unfocused + progress cleared with block, integration-off degradation with zero renderer errors); real-shell smoke on live PTYs: zsh A×2/B×2/C×2/D;0×1 and bash A×2/B×2/C×2/D;0×1 (bash init-file DEBUG-trap guard fixed after first smoke); terminal-adjacent E2E regressions green (m4 terminal lifecycle, quick console, external CLI; the one failure E2E-005 TS-intelligence reproduces on HEAD without these changes); boundaries + TypeScript + production build clean | Renderer-side OSC 133/9;4 parsing on each live xterm (`views/terminal-blocks.ts`, pure + host adapter) — blocks anchored by xterm markers, command text read from the buffer between B and C so any pre-existing integration also works; host-owned injection (`packages/terminal-service/shell-integration.ts` constants → written once per launch under userData, spawn plans resolved per terminal so a settings flip applies to the next spawn); unknown shell / disabled setting / missing dir spawn byte-identically to before (TERM-003). Three-surface progress paints one number: row ring ⇄ status-bar item ⇄ Dock `setProgressBar` (OSC 9;4 > conservative known-runner counters > honest indeterminate; determinate expires to indeterminate after 20s — no frozen bars). Command-finish notifications reuse PIVOT-014 hygiene (enabled-gated, PI_IDE_E2E-muted, focused-suppressed, one edge = one banner, dedup by blockId) and land the click on the block start (flash + select), not just the app; focused finishes ring the row bell instead. External sessions: observed-grade sessions mark enter/exit edges only; structured captures broadcast `external.turn` (Codex turn.completed / Claude result) as purple rail blocks — no fabricated turn boundaries. Block actions: copy output / save attachment / send to Room (reuses quick-console `draftStore.addTerminalRef`, YOU-authored, TERM-006) / rerun in the same PTY (user domain, no approval, TERM-005) with `rerunOf` links — history never overwritten (VER-005). New settings `terminal.shellIntegration` (default on) and `terminal.longCommandSeconds` (default 15) in Settings → Terminal. New channels: RPC `terminal.commandDone`/`terminal.progress`; events `terminal.revealBlock`/`external.turn` (versioned, strict). Honest limits: fish script untested on a live fish (not installed here; spawn plan unit-tested, degradation safe); observed-grade agent sessions get no per-turn blocks by design; `.prettierignore` now excludes `docs/design` (pre-existing check breakage from design-artifact commits). |
| SESSION-4B. Session as conversation — one room for chat + work, per-turn settlement (ADR-0032) | DONE (landed, awaiting owner review) | Unit 649/649 (+task-machine IDLE edges/terminal invariants; migration v6 upgrade fixture: worktree-settled→archived read-only, plain settled→IDLE, last-run settlement backfill; change-service rollbackToStates ×5: byte-exact restore/created-file deletion/conflict fail-closed/force/missing-blob); `npm run check` clean; e2e suites rewritten to the amended spec and green — m6/m7/m8/m9/m10, p2-visibility, p3-full-mode (auto-settle → settled-dock full rollback → same-room reply), timeline-window, memory, shell-v3/v4 (worktree: accept leaves main tree untouched, ARCHIVE merges — e2e proves the file lands only after archive), room-reviewbar (review surface retires on settlement), preview-gate (PR draft at accept, merge at archive, never committed), session-rail-polish, external-cli (settled Claude session stays live, resumes SAME task, History reserved for archived rows), replay suites (IDLE outcome), soak; screenshot walk `/tmp/charter-replay-semantic/4b-turn*.png` (turn 1 Accepted in the rail ledger while turn 2 runs and parks at review in the SAME room). Known pre-existing failure, NOT this change: m2-shell diagnostics (overlay renderer lost in the parallel ADR-0029 single-tree rework) | Owner picked model 4b after the follow-up-fragmentation report; two mock rounds (`docs/design/session-4b-continuous-room.html`) — round 2 removed turn separators and inline settlement cards (conversation stream stays pure; the ledger lives in the rail). Engine: `agent_runs.review_state/reviewed_at` (migration v6), task machine gains IDLE (settled conversation; ARCHIVED sole terminal), accept/rollback/plan-reject/zero-change turns settle to IDLE and the next message starts a new run in the SAME task (steerOrQueue/startTask gates + composer never says "starts a new Session" for live rooms; archived rooms keep the Home-parity continuation composer). Full mode auto-settles per turn (auto_accepted). Per-turn accept via `task.accept{runId}`; per-turn rollback `task.rollbackTurn` restores every path the turn touched to its boundary hash (newest-settled-first guard, preflight conflicts, blob-backed, chat-only turns settle by ledger); full rollback survives as the session action and now settles to IDLE (worktree full-rollback archives — the tree is gone). Worktree merge-back moved from accept to archive (`task.archive` v2 returns conflicts; taskStore confirm loop). `task.turns` channel exposes the per-turn ledger (prompt/diffstat/verification/review_state) — **API-level only after the acceptance amendment**: the rail Turns ledger shipped, then was removed on owner feedback 2026-07-20 (summary stays session-level like Claude Code/Codex; see ADR-0032 amendments). Replay outcome for IDLE ("轮次已结算 · 会话可继续"); `turn.rolledBack` ledger event + activity projection; rail badge Settled; answered milestone/dock/canvas conditions extended to IDLE. Historic rows migrate: plain ACCEPTED/ROLLED_BACK → live IDLE conversations; worktree ones → archived read-only |
| REPLAY-V3.1. Conversation-first recap — quoted conclusion, pivots, outward actions (ADR-0031) | DONE (landed, awaiting owner review) | Unit 619/619 (+11: pivot detection incl. progress-tick/same-key-lifecycle exclusions, outward recorded-app-only + dual-track result template, attention pinning by recorded risk/reversibility, conclusion verbatim quote + null case, inputs collection; parser TodoWrite→plan lifecycle + Codex todo_list→plan); `npm run check` clean; `replay-v3.spec.ts` 5/5 (new: plan-request-changes → pivot card with clickable id-backed grounds + pivot chapter on the axis; conclusion 引自最终报告, return line closes overlay, inputs cell, user bubble, 10k fold placeholder expands to samples); `replay-semantic-ui` 3/3 (assertion moved off the removed now-header), `p2-parallel-replay` + `external-cli` 7/7 green | Owner-approved mocks `docs/design/replay-v4-recap-mock.html` + `-noncode.html` implemented 1:1 minus honest downgrades: Recap left column renders user/agent/report facts as full-prose bubbles (activity `detail` ≤2000 now recorded for messages) with fold placeholders between kept nodes (count + kind stats + duration, 3-sample expand, jump to Explore) and violet pivot cards (recorded plan revisions; reason = the plan's own prose, refs = prior plan + failures since, id-backed); right column is the result card: outcome headline + dual-track deterministic sentence, quoted conclusion line (verbatim `report.final` excerpt, Inferred-labeled, citation chips), read-only return line (review stays room-only per ADR-0016), 重要变化/产出与对外动作 + 需要注意 duo with irreversible/high-risk outward actions pinned; contract adds 用时 (actual · story) and the inputs cell (user-attached code refs; memory/rule injections honestly "未入账本"). Cut as room duplicates: next-steps action panel, cost cells (RunDetails owns usage). Honest limits: interactive TUI sessions stay observed (no semantic layer); `irreversible` has no production source yet (gateway tool metadata is the upgrade path — badges render recorded three-state reversibility); external sessions still have no verification semantics |
| REPLAY-V3. One story, three depths — Recap / Explore / Verify (ADR-0017 Amendment 8) | DONE | Unit 398/398 incl. projection engine 19 (per-fact evidence levels, no session upgrade, Story Time grouping/idle-folding with mandatory floors, semantic chapters ≤8 by score, interval coverage, id-backed relations only, non-coding normalized fixtures, 10k-fact perf gate), ReplayService 10 (pagination cursors, cache, evidence belongs-to-task boundary, ask fail-closed, receipt manifest hash reproducible), renderer registry/filters 6; `tests/e2e/replay-v3.spec.ts` 4/4 (result-first no-autoplay, no A–E/numeric confidence, evidence ≤3 interactions, shared fact across depth + story/real, transport + keyboard, 1024/390 no-overflow + reachable Verify receipt, cited ask with boundary, receipt HTML+JSON export, honest verification states, approval relations + timeline Verify entry, Changes-panel path anchor, Home-card entry, 10k-event ledger search/scrub, console health); adapted p2/external replay regressions green; boundary 234 + TypeScript + production build clean | Phase 1: A–E removed; one controller (`views/replay/replay-controller.ts`) shared by Recap/Explore/Verify; explicit `ReplayRequest {taskId, depth, anchor(result/fact/change/path/actual-time), liveFollow}`; categorical evidence language + measured coverage band; numeric confidence and `replayGrade()` session upgrades deleted. Phase 2: main-side `ReplayService` over the same ledger (`task.replaySession` / `task.replayEvents` paginated ≤500 / `task.replayEvidence` with belongs-to-task boundary); event-broadcast-driven refresh replaces the 2s full poll; virtualized Explore list; renderer registry incl. document (.md/.txt prose compare) and spreadsheet (.csv/.tsv changed cells) over recorded blobs. Phase 3: evidence-bounded `task.replayAsk` (deterministic ledger narrative, citations validated main-side, fails closed; boundary states what the record cannot confirm — a model pipeline can slot behind the same fail-closed contract); `task.replayReceipt` HTML+JSON export with per-row SHA-256 + manifest hash, explicitly unsigned; live follow pill; entries: Home cards (View recap / Watch live), Changes rows (path anchor), resolved approval cards → Verify, Task Room/Agent Panel buttons. Honest limits: receipts unsigned (no tamper-proof claim); per-event redaction reasons not yet recorded by the external recorder (boundary notes shown instead); pre-existing `docs/design/*` prettier failures and the documented ⌘K flake are unrelated to this work. |
| REPLAY-V2. Unified evidence replay A–E (ADR-0017 Amendment 6) | SUPERSEDED by REPLAY-V3 (substrate retained) | Full unit/integration suite 367/367; boundary 217 + TypeScript clean; production build clean; managed replay Electron E2E and real-PTY external replay E2E green; live Claude Code 2.1.210 stream-json and Codex CLI 0.144.4 JSONL smoke green; visual QA at native 1440×900 for A–E and 1024×768 for A, including external observed A/D/E | One durable event/evidence ledger now drives five projections: A default cinematic, D long-task detail, E audit/approval, B observable causal map and C cross-app view. Pi is full capture; recognized Claude/Codex JSON streams are structured; ordinary TUI sessions are honestly observed. Per-write content blobs reconstruct intermediate file versions, bounded redacted terminal evidence is durable, playback follows wall-clock time at 1–16×, and external Task Rooms expose Replay. Structured JSON is filtered before documentary persistence, so reasoning/thinking envelopes never enter replay. No inferred app relationships. |
| APPEARANCE-3. Coordinated Terminal / Archive / Index skins (ADR-0020) | DONE (landed) | Full unit suite 358/358 (including Settings skin cases 5/5); m2-shell Electron E2E 3/3 (live switch + restart persistence); boundary(213) + TypeScript clean; production build clean; 1440×900 visual QA for all three skins and 1024×640 picker QA with 0 clipping/overflow and empty renderer console | `general.skin` is an independent persisted layer above existing light/dark/system brightness. Shared tokens switch shell colors, UI/display/mono fonts, shadows/radii and icon geometry; six Monaco themes, Prism semantic colors and six live xterm palettes complete the code/terminal surfaces. Archive is the backward-compatible default; custom editor fonts remain user-owned. No dependency or database migration. |
| FIX-EXTERNAL-SINGLE-RAIL. A promoted Claude/Codex terminal exclusively owns the right rail | DONE (landed) | Red-before/green-after Electron E2E on a real PTY: the prior UI kept `agent-panel` mounted beside `external-panel`; the fixed flow asserts Agent Panel visible before promotion → absent after promotion (including after its ⌘L toggle command) → visible again after Return to dock. Full external CLI E2E 4/4, renderer console/page errors empty, `npm run check`, production build, and screenshot `/tmp/charter-external-panel-resized.png` green. | The external session panel and generic managed-task panel no longer coexist. Promotion records the prior Agent Panel visibility and collapses it; Return to dock, terminal close, and workspace change restore that prior layout. A render-level exclusivity guard prevents commands from opening a second rail while the external terminal owns the slot. If Agent Panel was already hidden, it is not forced open later. Managed Task flows are unchanged. |
| FIX-EXTERNAL-PANEL-RESIZE. Promoted Claude/Codex panel has a usable horizontal splitter | DONE (landed) | Red-before/green-after Electron E2E on a real PTY: original splitter hit box measured 5px and failed the ≥12px usability gate; fixed flow promotes the terminal, drags the boundary 100px across Monaco/xterm, asserts panel width changes by >60px, then focuses the separator and resizes with ArrowLeft. Full external CLI E2E 4/4, renderer console/page errors empty, `npm run check`, production build, and screenshot `/tmp/charter-external-panel-resized.png` green. | Root cause: the boundary was a 5px absolutely-positioned hairline and used bubbling window `mousemove` listeners; it was extremely hard to acquire and could lose movement over Monaco/xterm. It is now a 12px hit target with Pointer Capture, persistent resize cursor/selection suppression, hover/focus/drag affordance, clamped 480–900px width, ARIA range metadata, and Left/Right/Home/End keyboard control. |
| FIX-EXTERNAL-RESUME. Claude/Codex external sessions resume through their CLI instead of the managed agent runtime | DONE (landed) | Real Electron + PTY E2E 4/4 green: ended Claude → Task Room → Resume writes `claude --continue` into the same terminal, returns the same Task to IN_PROGRESS, and creates no duplicate Task; crash/relaunch E2E: orphaned external Task recovers to REVIEW_READY, Resume creates a terminal in the recorded cwd and continues the same Task; existing accounting/room and native-installer detection regressions remain green. Command-map + IPC contract unit tests 9/9, `npm run check`, production build, and live persisted-data recovery green (the user's historical `external=ended + Task=INTERRUPTED` row normalized to REVIEW_READY on startup). | Root causes: restart recovery first projected external work as generic INTERRUPTED; session finish only closed IN_PROGRESS; older builds could leave an already-ended external row stranded as INTERRUPTED; the generic Resume button called `task.start`, which intentionally rejects external Tasks; Terminal → Room navigation did not bind `activeTaskId`. External metadata now records cwd and the verified CLI conversation UUID. Claude retains its native fallback for legacy rows; Codex only resumes with `codex resume <UUID>` and fails closed when no verified id exists. Codex rollout discovery supports both the CLI default home and an inherited Codex Desktop home, matches `session_meta.cwd` and launch time, and terminal spawning strips the host app's ambient `CODEX_HOME`. Startup also repairs historical ended/incomplete split-brain rows. Unsupported/custom CLIs do not get an executable Resume action. |
| FIX-HOME-TERMINAL. Home bottom-panel actions reveal the Editor | DONE (landed) | New focused E2E: Home → command palette → click New Terminal → Editor terminal dock visible and xterm mounted; Home → ⌘J → bottom panel visible. E2E-007 terminal lifecycle regression remains green; production build clean | Root cause: Home is a fixed overlay over the workbench, while `showBottomTab` and `toggleBottomPanel` only changed the hidden Editor layout. Successful bottom-panel actions now reveal the Editor before opening the panel; terminal creation failures still remain on Home and surface their existing toast. |
| SKILLS-LIVE. External Agent/Codex/Claude skill discovery + live sync (ADR-0019) | DONE (landed) | `skills.spec.ts` 2/2 E2E: original managed import/audit/slash flow plus isolated fake-home Agent Skills source → discovered Off → trust + auto-enable → watcher-driven SKILL.md update/new skill/delete reflected in the already-open Settings UI and slash picker; 25 focused skill/IPC unit tests; full 355 unit tests; full E2E 79 passed + 9 environment-gated/skipped; `npm run check` + production build clean. The repo-level `npm run test:security` remains non-runnable because the pre-existing M11 `vitest.security.config.ts` is absent; feature security cases run in the normal suite. | `SkillStore` is now a derived multi-source catalog: managed snapshots + `~/.agents/skills` + `~/.claude/skills` + `~/.codex/skills` + custom connected roots. External discovery is default-Off; source trust and auto-enable-new policy persist in lenient `skills.json` v2 while legacy managed ids/flags survive. Watcher events debounce to full scans; startup/on-demand/focus/45s reconciliation closes dropped-event gaps. External add/update/delete/rename follow the source directly; custom Disconnect never deletes originals; Import Copy stays isolated. Stable source/path ids, deterministic `name@source` conflict qualification, provenance/live/invalid/needs-review/revision UI, block/folded frontmatter support. `realpath` containment closes skill-root/nested-symlink escapes and retarget races in both audit and load_skill; invalid size/link revisions fail closed. Successful loads include source, catalog revision and exact content SHA-256 in results (source+revision also land in the persisted tool summary). Reused sessions receive a current catalog refresh each run/reply; Pi adapter/runtime contract unchanged. E2E only scans external roots when an isolated `PI_IDE_SKILLS_HOME` is supplied, never the developer's home. |
| UX-ROOM-E. Room conversation presentation — preset E「手记工作台」(ADR-0018) | DONE (landed) | real-room-e.spec 1 REAL-gateway E2E (agent write task on anthropic/claude-haiku-4-5 with a substantial Chinese brief → worklog groups with mm:ss clocks visible mid-run → plan/permission gates auto-decided → REVIEW_READY; docs/NOTES.md + src/util.ts `mul` asserted ON DISK; follow-up reply run updates the file again「更新于第二轮」and yields a second worklog group; computed-style assertions: spine ::before 1px, agent prose font contains Charter, milestone label uppercase, reply textarea outline none; screenshots /tmp/ui-shots/e-\*.png); mock-runtime regression m6-agent-ask 4 + room-peek 2 + room-reviewbar 2 all green; 345 unit tests; `npm run check` clean | Mockup-first (docs/design/room-conversation-directions.html — four pure directions A–D + hybrids E–G over one 37s scripted animated run, five mixable skin dimensions via a Tweaks panel; user picked preset E 1:1). room.css + RoomTimeline.tsx only: centered 640 column with a left time spine (milestone glyphs mask onto the rail; labels mono-uppercase with trailing hairline), agent prose serif (--font-display 15px/1.9, inline code pinned 11.5px), contiguous tool/verification/setup rows grouped into recessed mono worklog blocks (`tl-worklog`) with clocks counted from the room's first event + flash-in (reduced-motion aware; null-rendering events never break a group, cards/milestones/bubbles do), review bar + composer ride the same column, activity strip becomes a mono console ticker. Composer focus bug root-caused and fixed: theme.css global :focus-visible (near-black --focus-ring) beat `.tr-cinput{outline:none}` because main.tsx imports CSS after components (ESM hoisting → theme.css evaluates last; equal specificity → later wins) — now `.tr-cinput:focus-visible{outline:none}` + focus expressed on the card (border tint + 3px ring), matching Home. Row testids unchanged; Editor agent panel untouched |
| EXTERNAL-CLI-REV2. External session interaction redesign —「原地装饰、意图升格」+ xterm mount substrate (ADR-0017 Amendment 2) | DONE (landed) | external-cli.spec 2 E2E rewritten & green (detect = bar/badge only, dock untouched → user "Move to side panel" → scrollback follows + typed keys echo via PTY → session end keeps placement (panel ended state) → "Return to dock" restores pane+scrollback → bar ended + Review → room/peek; native-installer shape re-fixtured on a node-binary copy — the old /bin/zsh copy is AMFI-SIGKILLed and had voided that spec); NEW terminal-remount.spec (tab A→B→A, Home⇄Editor round-trip, still-live echo); REAL-CLI e2e 3/3 (claude 2.1.210 interactive incl. /exit + return, claude -p accounting → REVIEW_READY, codex 0.144.1 interactive incl. update-menu/trust prompts driven by keystrokes); full e2e 76/76; 346 unit; check clean | Mockup-first (docs/design/external-cli-attach-redesign.html — ~43s interactive film, user approved). Root cause of the shipped-broken 决策 4: xterm 6 `open()` attaches only on the FIRST call — every `innerHTML=''; open(host)` re-mount (promotion, dock tab switch, ⌘E round-trip, return) left a detached element = blank pane, dead input; `external-cli.spec.ts:89` was red at HEAD. Fix: shared `mountTerminal()` (first open, then `replaceChildren(term.element)` + fit + refresh + focus) used by dock/panel/room. Interaction: detection only decorates (tab badge + 34px session bar: snapshot chip, live counter, ⤢ Room, ⇥ Move to side panel; one toast); promotion = user click (slot-exclusive; pref `terminal.autoPromoteExternal` default off restores auto); panel 600px default (≥80 col TUI floor; was 350px ≈ 45 col), drag-resizable 480–900, ⇤ Return anytime; session end changes state (bar/panel ended + Review) but never placement; bar/room entry persist after end until the terminal closes or a new session replaces them; unpromote hands dock `active` back (fixes the returned-blank bug); kill/workspace-switch clear session UI state. Security semantics, accounting, room form, REVIEW_READY unchanged |
| EXTERNAL-CLI. External CLI agent sessions — embedded claude/codex terminals as first-class tasks (ADR-0017) | DONE | external-cli.spec 1 E2E against a REAL PTY (fake agent node script on PATH → detection badge ✳+EXT → "Open session room" → terminal center column in the room → watcher-accounted rail row → peek on the diff → session end → REVIEW_READY, changeSet honest vs the entry snapshot: modified +1, disk edit real); new unit tests: agent-detect 7 (tracker debounce, title/command matching), git snapshotTree/readTreeBlob 2 (temp index: dirty+untracked covered, real index untouched, .gitignore honored), external baselines 4 (bytes-baseline vs disk, created-null, idempotence, byte-exact rollback), isAccountablePath 3 — 337 unit total; `npm run check` clean; full e2e 69 passed + 1 known ⌘K load-flake (passes standalone) | Mockup-first (docs/design/external-cli-final-mock.html — composition of the two direction studies external-cli-terminal-directions.html × terminal-placement-directions.html; user approved). Detection: `pty.process` foreground-title polling (700 ms) + argv process-tree fallback whenever the title is neither a known CLI nor a shell prompt — covers node/bun interpreters AND the native installers' version-named binaries (`claude → versions/2.1.209`; ADR-0017 amendment 2026-07-15), one shared `ps` snapshot per tick, exit debounced ×2, `PI_IDE_EXTERNAL_CLIS` override; TerminalManager.onAgentState (kill/exit fire the edge too). Session start: `git write-tree` against a TEMPORARY index (untracked covered; user's index untouched; non-git degrades to first-seen baselines) → createExternalTask (mode ask, model external/<cli>, migration v3 `external_json`; startTask refuses external tasks — they never dispatch an agent run) → per-session WorkspaceWatcher; first touch of a path captures `ensureBaselineFromBytes` from the snapshot blob, so net change set, review presentation and byte-exact rollback are the EXISTING CHG machinery unchanged. Renderer: externalStore (toasts, terminal-tab ✳ badge + EXT chip + "Open session room" entry, session-end summary line in the scrollback); synthesized activity 'write' items feed the shared presence machinery (tree/task glow, rail filesTouched, peek pulse-follow). External Task Room form: the session's terminal (same xterm instance re-parented, PTY uninterrupted) replaces timeline+composer; EXT·unmanaged + snap-ref header, LIVE·following pill (peek auto-follows the file being written; click to pin), diffstat rail rows; Stop/Replay/composer hidden; end → REVIEW_READY (never auto-ACCEPTED; zero-change → Answered per PIVOT-031); app-quit strandings swept to review on startup. Bounded v1 per ADR-0017: no transcript parsing (codex app-server / claude stream-json recorded as the end-state path), no composer→stdin bridge, worktree terminals excluded from accounting |
| FIX-INIT. Renderer init idempotence (StrictMode duplicate IPC listeners) | DONE (landed) | init run-once 2 unit tests (320 total); `boundaries+tsc` clean; dev relaunch log free of MaxListenersExceededWarning (was: 12 `evt:workspace.changed` listeners, warning at 11); m2-shell + m3-editor E2E 5/5 (workspace.changed → editorStore reset/restoreTabs exercised) | Root cause: Workbench mount effect drained `initRegistry` with no run-once guard while React.StrictMode double-invokes dev effects; 4 of 8 `workspace.changed` subscribers (workspaceStore, editorStore, m4 inline, wirePython) lack per-store `initialized` guards → registered twice. Fix: registry + `runInitsOnce()` extracted to workbench/init.ts (module-level guard, returns whether this call initialized so the ADR-0013 gitStatusStore import stays gated); Workbench re-exports `initRegistry` so contrib imports are unchanged. Dev-only symptom (prod React doesn't double-invoke; 8 listeners < 10 cap) but dev double-execution of reset/restoreTabs/loadProject/lsp.status is gone |
| UX-ROOM-ENDING. Room completion as review bar + reply model/effort override (ADR-0016) | DONE | room-reviewbar.spec 2 E2E (bar lifecycle: Done milestone, evidence meta incl. unverified flag, review-open round-trip, overflow rollback → bar vanishes; reply re-points model → tl-model-changed note → next run → composer pill agrees); soak 50 laps green through the bar's overflow rollback; regression pivot-shell 3 + shell-v4 5 + m6 4 + m9 3; mock setSessionModel unit test (321 total); boundaries+tsc clean | Mockup-first (docs/design/room-ending-directions.html; user picked direction B + reply-composer model parity). Room-only presentation change — the Editor agent panel keeps its dense report card. `report.final` renders as a quiet ✓ Done milestone; a review bar docks above the composer while REVIEW_READY ∧ ¬answered (recorded evidence only: changed ±, checks incl. superseded/stale, unverified/risk warn rows; primary Review changes; agent summary + double-confirmed rollback in the overflow) and vanishes with the state. The mid-task reply composer gains the shared ModelEffortControl (trust level stays read-only — the session's tool catalog is mode-scoped at creation); `task.message` carries an optional ModelRef applied runtime-first (pi AgentSession.setModel + clamped setThinkingLevel; mock records it; new setSessionModel worker req/resp), then persisted to tasks.model_json + a `task.modelChanged` timeline note; launches re-assert the task model on reused sessions (stale/lost sessions recreated). A failed switch rejects the send loudly — nothing is ever sent on the wrong model |
| SKILLS. Managed skills + "/" invocation (ADR-0015) | DONE (landed) | skills.spec 1 E2E (seeded store → "/" picker filter/insert → /skill: task through mock runtime → Settings manager toggle/audit → Off skill vanishes from "/" immediately); 15 new unit tests (skill-store 10: frontmatter/import validation+limits/slug collision/enable round-trip/preamble excludes explicit-only/expansion incl. newline args+disabled pass-through/traversal+binary guards/remove; tools-skill 5: R0-in-ask/reference files/traversal/unknown-names-enabled/binary); 318 unit total; `npm run check` clean; real-app screenshot walk reviewed (picker, filter, insert, manager, audit, toggle) | Mockup-first (docs/design/skills-manager-mockup.html, user-confirmed two-state Off/Auto). AG-014-preserving design: skills live in a managed store (userData/skills; AppPaths.skillsDir) imported explicitly from local folders — project dirs are NEVER scanned. Pi's native skill injection is read-tool-gated (unusable under TOOL-001), so loading is product-owned: R0 gateway tool `load_skill` (name+optional bundled file, traversal-guarded, timeline-audited = AG-014 auditability), `<available_skills>` in buildPreamble (enabled ∧ ¬explicit-only), and `/skill:name` expanded in task-service (launch + steer/followUp; timeline keeps the user's raw text) — zero pi-adapter changes, works with the mock runtime. Frontmatter `disable-model-invocation` honored (explicit-only badge; still expandable via "/"). 5 IPC channels (skills.list/import[dir seam]/remove/setEnabled/read), SkillStore (SecretService-style folder store, 500-file/20MB import caps), Settings → Agent → Skills manager (Off/Auto segment, audit view with script highlighting + Permission-Engine note, two-step remove via confirm), composer "/" picker shared by Home + Task Room (enabled skills only, filter-as-you-type, explicit-only badge). Toggles apply to running sessions at next load_skill call (provider resolves live); preamble snapshot is per-session |
| SHELL-V5. Room zoom continuum — in-room file peek + ⌘E continuity (ADR-0014, PIVOT-034..036) | DONE | room-peek.spec 2 E2E (rail/timeline → peek beside the conversation, diff+file modes, Esc restore, explicit escape hatch → Editor tab; draft+peek survive room→Editor→room); shell-v4 worktree test extended (peek File mode reads WORKTREE content, escape hatch hidden); peek reducer 10 unit tests (313 total); `npm run check` clean; affected suites green (shell-v3 5, shell-v4 3+, home-v2, home-dragref, p2-visibility) | End state per ADR-0014: one room, three zoom levels, conversation is the anchor — no plain file click leaves it. New `task.peekFile` channel → `contextForTask().documents.readLogical` (mount-honest: worktree/non-focused projects/live buffers; binary/missing/1MB-truncated flags). `appStore.peek` (pure logic in views/peek.ts) + `FilePeek` split panel (Changes hunks reuse FileLens renderer; File mode = read-only Monaco, pulse-following); rail rows/in-room LiveBoard tiles/timeline evidence paths (now clickable)/⌘K-report chips via openWorkspaceFile/Home tree all default to peek in-room, ⌘/alt-click = explicit Editor; worktree tasks hide the hatch. ⌘E room-aware (openTaskInEditor shared with the header button); per-task reply drafts (draftStore) + timeline scroll memory (scrollMemory, pinned-to-bottom aware) shared by RoomTimeline and the Editor panel. Deferred by design: spot-edit in peek (needs attribution ADR), PIVOT-037 shell unification |
| UX-COMPOSER. Follow-up composer parity + merged model·effort control | DONE (landed) | `npm run check` clean (prettier+boundaries+tsc); 293 unit tests; affected e2e green (home-v2/pivot-shell/p2-visibility 8/8); real-app screenshot walk reviewed (Home + follow-up composers, both popovers; effort correctly greyed for unsupported levels) | Mockup-first (docs/design/composer-followup-model-effort-mockup.html, user-confirmed): (1) model + reasoning effort merged into ONE popover pill — shared `ModelEffortControl` (views/ui.tsx): provider-grouped model rows + an effort segment scoped to the selected model's supported levels, clamp-on-switch — replacing the two free-floating `home-thinking`/`home-model` selects so effort always reads against its model. (2) The Task Room follow-up (closed→new task) gets full Home-composer chrome (project chip, @-attach picker, trust segment, merged pill, send) seeded from the finished task's mode/model but freely editable; running/plan-awaiting states keep the bare reply pill (nothing to re-pick mid-run). e2e `home-model` readiness migrated select→pill (`toHaveValue`→`toContainText`); real-gateway `selectOption`→popover click. No mic button (app has no voice input — would be fake UI) |
| UX-DRAGREF. Sidebar New project entry + drag-to-@ context feeding | DONE (landed) | home-dragref.spec 3 E2E (sidebar dialog from Launcher AND from a Task Room; tree→composer file+dir chips with dedupe landing in the charter; tree→room reply inline @ at the caret); regression home-v2/shell-v3/shell-v4/pivot-shell/p3-light-edit 18/18; `npm run check` clean; screenshot walk reviewed | Mockup-first (docs/design/sidebar-newproject-drag-at-mockup.html, user-confirmed): (1) Projects section gains a New project… row (below Open folder…, which now carries the folder icon) opening NewProjectDialog — open state moved to appStore and rendered by HomeShell so the entry works beside a Task Room too. (2) HomeProjectTree rows are drag sources (views/dragRefs.ts: `application/x-charter-ref` MIME, dirs get a trailing `/`, `text/plain` `@path` fallback); the Home composer's onDrop short-circuits internal drags into addRefs (no relativize round-trip; dir chips render the folder icon), and the Task Room reply accepts the same payload by inlining `@path ` at the caret with a drop ring on the pill (`tr-composer.drop`) |
| P4-REVIEW. Review v2 + git decorations (ADR-0013) | VERIFIED | p4-review-v2.spec 2 E2E (Monaco diff + hunk decisions + request-fix round-trip; explorer/tab/gutter decorations on a git fixture); m8-review 4 + m9 3 + shell-v3 pass UNMODIFIED against the new surface; gutter-diff parser 5 unit tests; full suite 60/60 | Review overlay re-rendered as Changes list (A/M/D/R ± counts, file accept/reject) + Monaco side-by-side diff (baseline snapshot vs current via new task.reviewFile — ChangeService blobs, never git); hunk strip keeps content-derived keys/testids/hash guards byte-identical; “Request fix” sends selected lines + note back as steer and reopens the room; shared gitStatusStore → explorer letters+folder dots, tab marks, gutter change bars vs index (refresh on watcher/save); composer mode segment compacted (Read/Approve/Auto/Full) after a visual-walk audit |
| P3-FULL. Full autonomy mode (ADR-0012) | VERIFIED | p3-full-mode.spec 2 E2E (R3 without pause → auto-ACCEPTED → post-accept rollback byte-exact; failing verification pauses auto-apply); permission-engine full-mode unit test; task-machine ACCEPTED→ROLLED_BACK test; full suite green | 4th mode 'full': composer danger segment, R1–R3 auto-allow (standing denies + R4 + gateway boundaries untouched), plan auto-approve, finalizeRun auto-accept (actor system:full-auto) with honest fallbacks (failed/timeout verification → REVIEW_READY + AUTO_APPLY_SKIPPED + attention ping; merge conflicts → merge-blocked); ACCEPTED→ROLLED_BACK transition + Task Room post-accept rollback (non-worktree); notifications: quiet pass-through, 'Completed & applied' on ACCEPTED; spec §4/§6.1 + CLAUDE.md amended |
| P2-VIS. Thinking + live activity (ADR-0011 amends AG-006) | VERIFIED | p2-visibility.spec 2 E2E (collapsed thinking → expand → evidence exclusion; activity strip narrates); shell-v4 live presence (1440×900 + 900×900, two live files, live file → Diff); full suite green | thinking.delta/completed contract events; pi adapter forwards pi thinking streams per block (was dropped); mock 'thinking' step in ask-basic; settings.agent.showThinking (default on) gates stream+persist; room: shimmering live block folds into 'Thought for Ns' row; activity strip (pulsing dot + current action + tool elapsed + ↑↓ tokens); the same event stream powers the Session Rail ticker, Launcher per-Session Live Board, Room live tool row, and Session file heat tiles with decay/rhythm/writing beacon; excluded from report evidence |
| P1-QOL. Markdown prose, live tree, effort×model, new project, worktree supply (ADR-0010, ADR-0009 am.2) | VERIFIED | project-create 7 + worktree-include 11 unit tests; shell-v4 branch-chip assertions migrated; full suite green after each batch | Shared <Markdown> (react-markdown+remark-gfm pinned, no raw HTML, monaco-colorized fences, checked external links) across bubbles/plan/report/question; HomeProjectTree shares workspaceStore (fs.batch live refresh) + mergedBack glow pulses; ThinkingLevel+xhigh & per-model supportedThinkingLevels (pi-ai pinned) with hidden-unsupported composer select + runtime clamp; workspace.createProject (empty/git-init/clone non-interactive) + Home dialog; TasksView segmented restyle; worktrees: .worktreeinclude copy, setup command (lockfile suggestions, timeline evidence, fail-fast discard), slug branches, chip menu (terminal at worktree cwd, reveal), Changes→FileLens, orphan sweep, missing-worktree degradation |
| --- | --- | --- | --- |
| UX-POLISH. User-audit fixes (task cleanup, effort in composer) | DONE (landed) | Full suite 54/54 (soak/real-gateway/shots skipped); 268 unit tests; `npm run check` clean; manual visual walk (/tmp/explore2) | From a user-hat audit of the shipped shell: (1) task cleanup — `task.archive` finally has UI: hover archive on sidebar rows, archive on RECENT cards, Archive… (two-step) in the Task Room header; answered tasks close out (accept-the-no-op) then archive; running/plan-waiting/unreviewed tasks never offer it (labels.canArchiveTask). (2) Reasoning effort is a composer control (`home-thinking`, ✦ levels) — Settings → Models → default thinking level was a dead setting (never applied; runtime silently used 'medium'): now seeds the composer and every creation path passes model.thinkingLevel end-to-end; room meta line shows `· effort …`. (3) Home RECENT section — finished work (Accepted/Answered/Rolled back/Cancelled) stays one glance away instead of a dead-empty launcher. (4) Composer seeds mode from Settings → Agent (was hardcoded 'edit'). (5) ⌘K fixes: task badges use presentedMeta (Answered, not raw Review), New Task lands in the composer even from a room, Open Settings no longer yanks to the Editor surface. (6) `task.new` command + File-menu accelerator → ⌘N focuses the composer (old ⌘⇧I opened the legacy dialog); sidebar shows the hint. (7) Composer bottom-row overflow fixed (send button was clipped outside the card once effort joined; selects shrink with ellipsis, seg padding trimmed) |
| PROVIDERS. Multi-provider registry (ADR-0009 am.1, PIVOT-033) | VERIFIED | providers.spec E2E (presets + custom gateway + required-URL validation + independent delete); 8 model-catalog unit tests (anthropic/openai/openrouter/litellm protocols, evict); real-gateway spec ×2 live: Anthropic protocol AND OpenAI protocol (LiteLLM preset → 25 models fetched → real PONG via litellm/claude-haiku, screenshot real-4-litellm.png) | Presets Anthropic/OpenAI/OpenRouter/LiteLLM + custom Anthropic-/OpenAI-compatible gateways; protocol/endpoint/name = non-secret meta beside keychain key (legacy entries infer protocol); effective endpoints resolved in main; pi adapter registers non-builtin providers lazily with wire protocol + apiKey (registry requirement); Home picker groups models per provider; catalog evict on delete |
| SHELL-V4. Global tasks, multi-mount, worktrees (ADR-0009, PIVOT-028..032) | VERIFIED | shell-v4.spec 5 E2E (persistent shell; cross-project approve/finish while another project is focused; worktree isolation + merge-back with main-tree byte checks; composer request-changes → plan v2; heartbeat ticker + rail focus board); full suite 53/53 (soak/real-gateway skipped); 265 unit tests (light-completion presentation, answered notification copy) | Engine: per-root ProjectContexts (gateway/permissions/changes/verification per mount — closes the cross-project gateway-rebind hazard), WorktreeService (create/discard/merge-back with baseline conflict preflight), task.create{projectPath,isolation}, task.list scope=all, TaskDto{project,changedFiles,worktree}, migration v2, plan request_changes channel (works for real runtimes via PLAN_CHANGES_REQUESTED tool result), pi adapter now delivers the system preamble (identity + mode rules were silently dropped before). Shell: persistent sidebar (grouped global tasks, action ticker heartbeat, Inbox, project badges), Task Room v2 (project·branch chip, rail THIS-TASK LIVE board, timeline v2 mockup language, plan-aware composer), light completion (Answered/Done, excluded from Needs You) |
| 1. Engineering baseline | VERIFIED | E2E m1-baseline (isolation+IPC), 28 unit tests, packaged .app launch smoke, boundary lint | node:sqlite/node-pty/rg strategies in ADR-0003 |
| 2. Shell and persistence | VERIFIED | E2E m2-shell (restart restore), m2-db-failure (safe mode), 10 unit tests (db+settings) | |
| 3. Workspace and editor | VERIFIED | E2E-002/003 pass (m3-editor.spec), 31 unit tests (doc store/workspace/path boundary), 10k-file lazy tree integration test | Data-loss race renderer-guard documented in editorStore |
| 4. Search, LSP, terminal | VERIFIED | E2E-004..007 pass, 6 search unit tests, full E2E suite 12/12 twice | rg via system binary (ADR-0003); python LSP degrades with guidance |
| 5. Git and changes | VERIFIED | E2E-008 + non-git flow, 51 unit tests incl. 34-case rollback matrix | Full 50-case matrix expands at M12 gate run |
| 6. Pi read-only agent | VERIFIED | E2E-009/019-core/HIST-002 (4 E2E), pi adapter contract tests (no-builtin-tools), 141 unit tests total | OAuth providers deferred to API-key flow (ADR pending M12 notes) |
| 7. Tools and permissions | VERIFIED | E2E-012/013 + approve-and-run + ask_user (4 E2E green), permission engine 15 tests, classifier 17, runner 11, command tools 9, security matrix 9, SQL store 3 | R3 never granted permanently; R4 refused pre-prompt; PERM-007 param-binding enforced |
| 8. Agent editing and review | VERIFIED | E2E-010/011/014/015 all green (m8-review.spec, full suite 26/26), 25 new unit tests (review hunks 10, write tools 10, plan utils 5) | Plan approval flows through the propose_plan tool channel (works for real Pi too); write gate denies first write until plan approved (AG-007); hunk keys are content-derived so decisions fail closed on staleness |
| 9. Verification and history | VERIFIED | E2E-016/017/018 green (m9-verification.spec, full suite 29/29 twice), VerificationService 7 unit tests, rename tool test | Final report separates agent narrative from system evidence; stale/superseded semantics on verification_runs; unverified accept needs explicit confirm; rollback preflight stops on external-change conflicts |
| PIVOT. Dual-form shell (ADR-0004) | VERIFIED | pivot-shell.spec 3 E2E (PIVOT-001..009), full suite 32/32 twice, model-catalog 4 unit tests | Charter branding (no Pi in UI); Home task launcher default entry; Settings provider keys + live model fetch |
| HOME-V2 P1. Activity stream + mission control (ADR-0006) | VERIFIED | home-v2.spec 3 E2E (PIVOT-011..015), full suite 35/35 twice, activity projection 9 + notification 4 unit tests | Pure-projection activity stream (live = replay source); mission-control cards with live current action; macOS notifications on attention edges (focused-window suppressed); drag/@ context refs; clickable paths; tool lifecycle renders as one in-place card (callId dedupe) |
| HOME-V2 P2. Parallel runs + replay + glow + ⌘K (ADR-0006) | VERIFIED | p2-parallel-replay.spec 3 E2E (PIVOT-016..018), full suite 38/38 ×3 consecutively, gateway modeForTask unit test (252 total) | maxConcurrentRuns (default 3, FIFO beyond); per-task mode resolution replaces the mutable gateway.mode; action-centric session replay (scrubber + stored per-step patches + file lens, read-only); presence glow on tree/task rows from change events (no polling); ⌘K over projects/tasks/files/actions |
| HOME-V2 P3. Light editing (ADR-0007, PIVOT-019/020) | VERIFIED | p3-light-edit.spec 2 E2E, full suite 40/40 ×3 consecutively after fixes | Rich Markdown via @mdxeditor/editor@4.0.4 (opt-in per file + setting; writes through the SAME Monaco model → identical dirty/conflict/save guarantees; lazy-loaded + error boundary; Prism global shim). In-house canvas image annotator (arrow/box/true-pixel mosaic, save-copy never overwrites, attach-to-charter). Root-caused the earlier full-run flakes: (1) E2E runs were emitting real macOS notifications — now silenced under PI_IDE_E2E; (2) ⌘K hover-on-render stole keyboard selection when the physical cursor overlapped the palette — onMouseEnter→onMouseMove; (3) rich-editor mount settle window swallowed instant edits — replaced with a normalization-baseline guard |
| SHELL-V3. Task-centric shell (ADR-0008, PIVOT-021..025) | VERIFIED | shell-v3.spec 3 E2E (Task Room flow, entry consolidation, Live Board); full suite 46/46 ×2 (soak skipped); state-labels 5 + live-board 4 unit tests (262 total) | P1 humane vocabulary (shared dictionary, no raw enums/emoji; data-state for tests), danger hierarchy (two-step rollback, Cancel-task demoted), review-overlay collision root-caused (undefined --bg); P2 Studio tokens ported 1:1 from direction-picker.html (light+warm dark, serif hero anchored to composer, segmented trust control + caption, quiet status bar); P3 Task Room on the Home surface (timeline/cards shared with the Editor panel via TaskTimeline exports; changes+verification rail; decision panel; Review/Replay global overlays; submit→room, no auto-jump; Editor entries: sidebar row + ⌘E + room header); P4 Live Board (event-driven tiles: ripple/heat 60s decay/rhythm/writing beacon; per-task boards; diff-so-far FileLens with pulse-following refetch; pauses on blur/reduced-motion; edit-live mock scenario); P5 provider base URLs (PIVOT-026: keychain key + non-secret endpoint, catalog fetch via gateway, pi adapter registerProvider + synthesized gateway model ids — verified live), Settings Studio restyle, Home project tree (PIVOT-027), Advanced Title parity (PIVOT-012r). Amendments in ADR-0008 |
| 10. Recovery and diagnostics | VERIFIED | m10-recovery.spec 3 E2E (E2E-020/022 + renderer crash), soak.spec 50-task run (opt-in), disk-write-failure unit test, full suite 43/43 ×2 (soak skipped) | Worker orphan guards (port close + ppid watchdog); ordered will-quit teardown (gates resolved → worker disposed → DB closed last, fixes "database is not open" on quit); INTERRUPTED recovery entry (Home + AgentPanel Resume/Review/Roll back); orphaned pending permissions cancelled in the event log on restart (no replay); redacted support bundle (diagnostics.supportBundle); E2E crash dialogs/notifications suppressed under PI_IDE_E2E |
| 11. Security and quality hardening | VERIFIED | `test:security` now covers 139 Vitest cases + 2 Electron boundary cases; `test:perf` 6/6; M12 dependency patching removes the prior DOMPurify/protobuf/js-yaml/brace audit findings and enforces actual installed resolutions; `npm audit` 0 vulnerabilities | Full matrix in `docs/M11_SECURITY_PERFORMANCE_REPORT.md` |
| 12. Packaging and release | VERIFIED (Beta 5 candidate) / BLOCKED (signed Stable qualification) | Beta 5 candidate; full release gates; E2E-023/024; native mac/win/linux CI; DMG/ZIP/NSIS/tar.gz; clean install smoke; SPDX SBOM/licenses/notices/checksums; GitHub Prerelease workflow; ADR-0048 update service, typed IPC/UI, `latest`/`beta` feeds and pre-install SQLite backup; unsigned Stable rejected by policy | Beta 5 checks packaged builds and shows the verified Release page; signed macOS/Windows can additionally background-download and explicitly restart/install, while unsigned previews/Linux never replace the app. Paid certificates/notarization, real signed update qualification, real-provider evaluation and owner sign-off remain Stable blockers |

## Current blockers

No blocker remains for the unsigned GitHub Beta. Signed/notarized Stable separately requires paid
Apple/Windows credentials plus the real-provider product evaluation and owner sign-off.

## Latest test evidence

| Date | Commit | Command | Result | Artifact |
| --- | --- | --- | --- | --- |
| 2026-08-09 | (Diff 面板默认收起 + 可收起, landed) | prettier（本批 6 文件全清）+ `tsc --noEmit` 清 + e2e：session-canvas "keeps one shell"（新增默认收起/✕ 收起/再点重开断言）、a11y-zoom-diff A11Y-005、external-cli:998、room-peek diff 段、code-context 全过 | 用户报 Diff 页默认自动展开第一个文件且无法收起，判"设计有点乱"；四方向 mock（docs/design/diff-panel-redesign.html）评审后用户选定方向 D（最小修正）。实现：①去掉 `selected ?? changeFiles[0]` 兜底——不点文件不展开任何 diff；②diff 卡片表头新增 ✕（Collapse diff，i18n 收起差异），点 ✕ 或再点当前文件行均收起；收起为视图局部态（dismissed），不动共享 peek tabs，任何新 openPeek（rail 行/时间线/文件列表）仍自动展开；③选中文件行加高亮+左侧指示条。**归因备注**：同批环境跑出的 a11y-zoom-diff A11Y-003（Settings Close 按钮超时）经 stash 对照实验证实无我改动一样挂（预存问题）；room-peek:65 editor tab 失败发生在并行会话在制的 editor-tabs 重构面（editorStore.ts/editor-tabs.ts 均为我开工后出现）；session-canvas:223 分栏重进丢失为已挂账 ADR-0055 池回归 | apps/desktop-renderer/src/views/SessionToolCanvas.tsx; docs/design/diff-panel-redesign.html; /tmp/charter-session-canvas/diff-panel-1440.png |
| 2026-08-09 | (i18n 标识符保护 + 运行中会话尾栏移除, uncommitted — awaiting user acceptance) | `npm test`（1385 全绿）+ 新 `tests/e2e/i18n-identifiers.spec.ts` 真机通过（zh 界面下 chrome 已翻译、分支 main/文件名保持原文）+ session-canvas/agent-catalog/external-cli:1453 相关 e2e 复跑 | 用户报：Settings 中文时分支名 "main" 被显示成"主要"。根因：i18n 批次的 DOM 级自动翻译层（i18n-dom.ts）对任何命中词典键的独立文本节点整词替换，而标识符容器不在排除边界内，且词典混入了纯数据词条 `main: '主要'`。修复：①删除 `main` 词条；②把标识符/数据容器（`.mono`、`.pt-row`、`.pc-tree-row`、`.pc-header-git`）划入不翻译边界；③混合容器精准豁免（会话头项目名、SCM 分支列表加 `data-i18n-ignore`）——CommandPalette 等含 UI 文案的共享类未整类划界以免误伤翻译。另按用户要求移除外部会话运行中的纯信息尾栏（"Session live — Open the terminal…"，零操作零新信息），会话区拿回该高度；托管运行中（有 Stop）、中断（Resume/回滚）、待决策、已结束（评审/PR）等承载真实操作的形态全部保留。顺带清掉 3 个 spec 里残留的"启动落编辑器面"旧契约点击（session-canvas/agent-catalog/readme-assets）。**归因备注**：session-canvas:216 分栏比例重进丢失为 ADR-0055 保活池回归（HEAD 基线通过；机制=池化房间不再重挂载，REVIEW_READY 重进不再触发工具自动展开），非本批引入，留待该批次会话修复。**追加（同日，用户指示"按最完美方式修复其余问题"）**：①词典空格拼接式译文全量清理——183 处"汉字 空格 汉字"（"远程 会话"→"远程会话"）按规则修复：仅去除汉字之间的空格，中英文/数字间的正确排版空格全部保留；脚本化处理 + 键完整性校验（1088 个键零变动，唯一中文键"简体中文"无空格不受影响）+ 抽样目检自然度；②两条早前报告项经深入核验为**误报并撤回**：SessionRail 嵌套 t(模板) 实际由 dynamicChinese 正则兜底正确产出中文（非"永不命中"）；运行中切语言实际已完整生效（App.tsx 以 key={locale} 整树重挂 Workbench，t() 与 DOM 层同步刷新）——已将 i18n-identifiers e2e 中的 reload 移除并真机证实免重载即时切换。残余已知边界（记录不修）：混合容器中的用户数据（任务标题等）若整体命中词典键仍会被 DOM 层翻译，需逐点 data-i18n-ignore 治理，属 i18n 批次后续 | apps/desktop-renderer/src/i18n-dom.ts; apps/desktop-renderer/src/i18n.ts; tests/e2e/i18n-identifiers.spec.ts |
| 2026-08-09 | (editor lives in context, ADR-0054, uncommitted — awaiting user acceptance) | `npm run check`（本批文件 prettier 全清 + boundary + tsc 清；全局 check 仅剩并行会话在制的 i18n.ts prettier 警告，未触碰）+ `npm test`（1374 全绿）+ 10 相关 e2e spec 32/32 全绿 | 起因：用户判定左下角全局 Editor 按钮"非常混乱"（一键三果：有会话→展开工具画布；有项目→编辑器；全无→空白死路），浏览器 mock 三方向评审后选定 A+C（编辑器归属上下文）；验收中追加两点：点项目后 rail Files 树仍空、树里点文件仍显示旧会话。实现：①ActivityBar 删除 Editor 按钮，底部仅剩 Settings；②appStore 拆除 `surface`/`setSurface` 兼容垫片与 `homePick` 钉（含 NavigationSnapshot 字段、全部 set 补丁、8 处调用点），workspace 打开一律不再自动导航（PIVOT-006 退役）；③项目中心 Files 标签嵌入真编辑器（共享文档模型 EditorArea，点文件即开即改即存；非当前项目保留只读预览 + 显式升级按钮）；④打开项目中心即 followProject 成为工作上下文（ADR-0046 原则从会话扩展到项目），rail Files 树/composer 随看随跟；⑤rail 树点文件一律全屏打开编辑器（PIVOT-027r 点击即 peek 退役），时间线证据路径仍走 peek，⌘/alt 改开 peek Edit 模式；⑥⌘K/palette 保留 "Open project files" 兜底（无项目→Projects rail，永不空白）。测试：launch helper 移除 Editor 进入点与自动落地等待；pivot-shell/shell-v3/shell-v4/home-dragref/project-center/project-files-restructure/session-rail-polish/terminal-remount/m3-editor 按新契约重写断言；workspaceStore.follow.test 与 appStore.nav.test 同步。归因备注：shell-v4 心跳用例曾在并行高负载下超时（mock 跑完早于断言），HEAD 基线工作树对照 + 平静环境复跑均绿，判定为负载敏感时序抖动而非本批回归 | docs/adr/ADR-0054-editor-lives-in-context.md; docs/mockups/editor-entry-redesign.html（方向评审 mock） |
| 2026-08-09 | (external-terminal scroll jank, ADR-0055 follow-up, uncommitted) | 真机探针 + `npm run check` + `npm test`(1385) + terminal-blocks/terminal-remount/external-session-scroll e2e | 用户报真实 Claude Code 长会话滚动仍卡——外部 CLI 会话的房间主体是 xterm 终端（ADR-0017"对话即终端"），不走已优化的转录路径。两个根因两个修复：①**标记轨每滚动行全量重渲染**——TerminalBlockRail 的 `term.onScroll(() => setTick(+1))` 无节流，而轨上的点只依赖 scrollback 基线（baseY）与视口位置无关，长 Claude 会话几百个块 = 每滚一行重算重渲染一整条轨；改为"仅 baseY 变化时每帧至多一次"。真机探针（200 块 + 3000 行回滚，100 步滚轮）：纯滚动期间轨重渲染 **51 次 → 1 次**，WebGL 保持。②**WebGL 上下文预算**——每个存活终端持一个 WebGL 上下文，Blink 默认 ~16 个即静默逐出最旧、被逐出的 xterm 回退慢速 DOM 渲染器（有界重试）；用户几十个会话恰好踩中"越用越卡"；主进程 ready 前加 `max-active-webgl-contexts=128`（ORCA 同款教训，其 configure-process.ts:274 有相同修复与理由） | apps/desktop-renderer/src/views/TerminalPanel.tsx; apps/desktop-main/src/index.ts |
| 2026-08-09 | (TUI wheel fidelity + terminal scrollbar visibility, uncommitted) | 真机 fiber 仪器探针（term.onRender/viewportY/滚轮时间戳逐帧采样，用户真实数据克隆 + 真 `claude --resume`）+ 新单测 terminal-tui-wheel.test.ts 7 例 + external-session-scroll/terminal-remount e2e + `npm run check` | 用户带截图复报"Claude 会话滚动非常卡、根本没有滚动条，Codex 却丝滑有滚动条"，且其实例已含此前修复——判定为未解真缺陷并重查。**根因（仪器实测定案）**：live Claude Code TUI 开启鼠标捕获（mouseTrackingMode=any、buffer 仅一屏无 scrollback），滚轮不走 xterm 滚动而是转成鼠标报文由 claude 自重画——而 xterm 6 的 MouseService._consumeWheelEvent 对触控板像素增量**先乘 0.3 再取整**（isLikelyTrackpad 分支），70% 手指行程被丢弃，实测同一手势下内容更新间隔 p95=184ms（≈5fps）＝"滚不动+一顿一顿"；Codex 不捕获鼠标走原生 scrollback（满帧+有滚动条），不对称由此完全成立。"没有滚动条"对 live claude 是协议必然（无 scrollback 可滚），对原生会话则是滑块默认色过淡（前景色 20% 透明度+5000 行下 20px 最小滑块+800ms 淡出）。**修复两项**：①`terminal-tui-wheel.ts`——ORCA 同思路的滚轮忠实度处理器（其 pane-terminal-mouse-wheel.ts 注释明言就是绕这个衰减）：鼠标捕获激活时按真实手指行程折算行数（像素÷CSS 格高，跨事件小数进位、零衰减；Alt/Ctrl=快滚档），以 deltaY=±0.95/scrollSensitivity 的行模式克隆重放（0.95 居 (0,1] 中部，浮点噪声不可能跨整数边界，恒 1 报文/事件；microtask 派发保序不限帧）；Shift 保留 xterm 原生"绕过报文直接滚"逃生门；mouseTracking=none（shell/codex/已结束会话）分毫不进此路径；②滑块主题色显式派生（terminal-visuals.withTerminalScrollbarColors：各皮肤前景墨色 32%/45%/55%，applyTerminalAppearance+makeTerm 两个咽喉点统一生效）。**A/B（同一真实 resume 会话、同一触控板手势仪器）**：慢滚渲染次数 34→83、渲染间隔 p95 184→77ms；回滚手势 31→76、176→76ms；打字重绘压力下 161→199、75→58ms；原生路径滑块滚动中 xterm-visible=true、rgba(46,52,52,0.32) 且随滚动移动（截图 /tmp/adr55/slider-mid-scroll.png）；带鼠标报文断言的 external-session-scroll e2e 通过=报文管道无回归。**已知边界（记录不修）**：claude 自身重画节奏（PTY 往返+其渲染）是剩余上限，~13-17fps 为 CLI 侧属性；无鼠标捕获的 alt-screen 应用（less/无鼠标 vim）走箭头键转换路径仍受 xterm 0.3 衰减，无用户报告暂不动。**事故披露**：本轮排查中两次操作失误——清理残留进程时误杀用户真机终端守护进程（其 live claude/codex PTY 随之终止，对话可 Resume 无损恢复）；原生腿探针把 `cat` 命令误打进克隆内 resume 的真实 claude 会话触发一次真实 turn（≈$0.48，该会话尾部多一条垃圾交换记录），均已如实报告用户 | apps/desktop-renderer/src/views/terminal-tui-wheel.ts(+test); apps/desktop-renderer/src/views/terminal-visuals.ts; apps/desktop-renderer/src/views/TerminalPanel.tsx |
| 2026-08-09 | (terminal scroll — ORCA parity cross-check + real-resume verification, uncommitted) | 真实场景探针 ×4（脚本存 /tmp/adr55 可复现）+ `npm run check` + external-session-scroll/mission-orchestration-v2 e2e | 用户报"resume 之前的 Claude Code 会话滚动还是很卡，不如 Codex"，指示对照 ORCA 源码"能抄就抄"。**排查结论：修复本身有效，用户测的实例不含修复**——机器上两个运行中的 Charter 进程分别启动于周四与今日 8:17，而外部终端两项修复（标记轨节流 + WebGL 预算）今日 11:54 才写入、12:18 才构建；需重启应用进入新构建。修复后构建上的真实测量（120Hz 满帧=8.3ms）：①真实 `claude --resume` 29MB 会话进房间滚动 p95 10.1ms/零长任务（注：resume 只重印约一屏，buffer 反而小）；②真实会话内容（中文长段/代码/真彩 SGR）灌满 5000 行 scrollback，WebGL 与 software 渲染器滚动+甩滚全部 p95≤10.3/零>17ms 帧；③88 个 rail 标记 + 持续 PTY 写入下边写边滚 p95 9.3/零长任务；CPU profile 空闲 85-93%。**Codex 顺/Claude 卡的不对称由此完全解释**：Codex TUI 走 alt-screen（滚轮转发给应用重绘，不经 scrollback 机制），Claude 走 normal buffer scrollback——旧构建里每滚动行全量重渲染标记轨只打击后者。**ORCA 对照（本会话逐行读其源码）**：xterm 版本两边完全一致（6.1.0-beta.287）；其 max-active-webgl-contexts=128、有界停靠、WebGL 失败回退+重试均已具备或已抄；本轮补抄其滚动手感项 scrollSensitivity 1.15 + fastScrollSensitivity 5（我们默认格子 15px×1.2 高于基线，ORCA 同理由注释 pane-terminal-options.ts:47；Alt+滚轮=5× 快滚），两条滚轮相关 e2e 复跑绿；其 TUI 滚轮报文倍增器（pane-terminal-mouse-wheel.ts）与 scroll-intent 吸底分类为行为正确性特性、非卡顿因素，记入后续对照清单未抄 | apps/desktop-renderer/src/views/TerminalPanel.tsx（scrollSensitivity）; /tmp/adr55/{claude-resume,big-buffer,stream-scroll}-probe.mjs |
| 2026-08-09 | (session switch continuity, ADR-0055, uncommitted — awaiting user acceptance) | `npm run check` + `npm test`（含新 taskStore.cache.test 7 例 + timeline-window openingWindow）+ 10 相关 e2e + `tests/perf/ui/switch-bench.ts` A/B | 用户反馈会话切换"像整页重建、要几秒、无现场保留"（对照 ORCA 的 visited 挂载 + display:none 切换 + 8/12 有界停靠，已逐行读实源码证实其形态）。三层实现：①**按会话时间线缓存**（taskStore.timelines，MRU 上限 8，活跃任务免逐出；openTask 改 stale-while-revalidate——缓存命中同步呈现零 Loading 闪烁，后台 revalidate 按事件 id+sequence **保留旧对象引用**，历史未变则数组引用不变=零重渲染；task.event 现应用到任何已缓存任务，隐藏保活房间后台保鲜；删除/归档清缓存）；②**渐进首屏**（开会话只挂 80 条尾部 PROGRESSIVE_WINDOW，滚近顶部 240px 自动 loadEarlier 复用既有锚定，按钮保留；有中部滚动记忆时用全窗口保 PIVOT-036 还原；e2e 的 __PI_IDE_TIMELINE_WINDOW 覆盖恒胜）；③**房间保活池**（SessionRoomPool，MRU 3 槽；每房间 portal 进池持有的稳定容器元素，切换=把活跃容器 reparent 进可见宿主、其余**整体摘出文档**——本仓库 mountTerminal 同款模式；弃 display:none 叠放方案是因为隐藏房间的重复 data-testid 会炸全部 Playwright 严格选择器与 a11y 树，detached DOM 对全局查询不可见且零布局/合成成本；React fiber/状态全程保留，揭示=重挂+从滚动记忆重放位置）。**冻结契约**：隐藏房间的一切活跃单例订阅（streaming/loading/changeSet/timeline context）在 selector 内钉常量，活跃会话的 token/事件/changeSet churn 对隐藏房间零重渲染；池外会话行为与旧版逐字节一致。**A/B 证据（双 400 事件重转录，各 3 轮）：改前每次切换 870-1300ms（冷热无差，每次全重建）；改后池内热切 118-137ms（≈7×），冷开 250-370ms（≈3×，渐进首屏）**。**对抗审查闭环（独立 verifier 真机装置，脚本存 /tmp/adr55 可复现）**：判 PARTIAL 后两项 CONFIRMED 均已修复并用原装置复验——①冻结失效（隐藏房间每轮 run 被 s.tasks 全量订阅连带重渲染 ~5 次）→ TaskRoomView/SessionRoomPool memo 化 + 每房间只订阅自己的 task 对象 + 编排快照对隐藏房间钉常量，复验：池边界版本 1→1 完全冻结，仅剩一次浅渲染且子树全部 bail；②渐进窗口开局误触发（吸底写入的 scroll 事件在"内容溢出<240px"形态下瞬间挂满全窗）→ 自动加载改为仅当 scrollTop 相对上帧真实变小，复验：原失败几何 723/109 下窗口 2.5s 稳定 + 真实上滚正常触发（4→27 行锚点无跳）。另修静态隐患两处：task.get 失败不再留空占位毒缓存、revalidate 保留快照后到达的广播事件与未落定的 live 工具行（各有单测）。check 清；全量单测绿（本批新增 11 例）+ 新增 tests/e2e/session-room-pool.spec.ts（池地标唯一性/无重载切回/草稿存活/cap-3 逐出回退缓存路径）全绿；e2e 子集 10 spec 中与本批相关的全部通过（timeline-window/p2-visibility 双会话/m6/m7/m8/external-session-scroll/session-rail-polish/room-hierarchy 多轮），5 个失败均已归因并行会话在制改动（roomCopy 改应用级 locale、编辑器面重构的 project-tool-back/tab 恢复），证据：git diff roomCopy.ts 非本批、失败点全在本批未触碰的面 | docs/adr/ADR-0055-session-switch-continuity.md; tests/perf/ui/results/pooled-switch-\*.json + head-baseline-switch-\*.json |
| 2026-08-09 | (work item document page, ADR-0053, uncommitted — awaiting user acceptance) | `npm run check`（本改动全清；仅剩 docs/mockups/ 两个并行会话的未跟踪 HTML 有 prettier 警告，未触碰）+ vitest workDates/workItemStore + `tests/e2e/work-board.spec.ts` + `work-board-theme.spec.ts` | 用户否决了 15 字段平铺 modal（“信息密度极高的填空 UI”），四方向 mock 评审后选定方向 D 并两轮补充（真日历、附件、去 Start、输入聚焦不出框）。实现：①WorkItemForm 整体删除，新 WorkItemPage 文档页统一新建/编辑——标题为衬线文档标题，Stage/Type/Priority/Deadline/Reminder/Labels 收为标题下一行 chips，Source+类型字段为静默属性行，验收/交付物为真清单；②新建=本地草稿（localStorage 防崩溃丢失，可恢复），带标题离开页面即建卡，无提交按钮，空草稿自动丢弃；编辑=逐字段 blur/选取提交，串行保存队列保住乐观版本门；③自定义日历弹层（周一起格、时间片、快捷项、可打字语法 workDates.ts 13 单测）替换全部原生 datetime-local；④新增 Attachments & links 区：粘贴链接/路径或经新 IPC `workItem.attachment.pick` 原生选文件，落为 file/link evidence（新建队列在建卡后立即固化），打开走 app.openExternal / app.revealPath；⑤页面内文本输入抑制全局 :focus-visible 描边（光标即聚焦态），按钮仍保留键盘环；⑥Start 字段从界面移除（契约与既有数据不动）。真机证据：4 条 e2e 全绿 23.8s（日历选期、附件→evidence、空草稿丢弃、过期提醒即触、六皮肤主题继承），Playwright 实机截图（/tmp/workpage-\*.png：新建空态/日历/填毕/落板开抽屉/编辑态）逐张目检通过。**追加（同日）：提醒通知真机取证 + 免授权增强**——逐秒截屏 14s + ncprefs 检查证实未签名开发版的 macOS 系统横幅被静默丢弃（应用从未完成通知中心注册；打包版未验证），应用内右下角弹卡为当前唯一可见通知；据此实现免授权增强：Dock 角标=未处理已到期提醒数（service.firedReminderCount，changed 回调刷新）+ 失焦时 dock.bounce('critical')（E2E 禁 bounce 不禁 badge），e2e 断言 badge 随 fire→1/snooze→0 真机通过；应用内提醒卡重设计（用户否决右下角静态卡）经动效 mock 两轮迭代后用户选定 ★A+B 组合并已实现：WorkReminderHost 改为右上堆叠"闹钟卡"——挂钉摆入（±8° 阻尼）、时钟图标持续摇摆、每 10s 心跳（上顶 5px+琥珀光晕）直到处理；View/Snooze/✗ 均为真实处置（✗=持久取消并联动 Dock 角标归零）；prefers-reduced-motion 全禁动效；删除重复的右下角 toast；探针抓到并修复"闹钟卡压住抽屉关闭钮"（锚点下移+容器 pointer-events:none）；e2e 增加动效名断言（swing+heartbeat）全绿，真机截图目检（/tmp/alarm-final.png）。**追加（同日，用户实测反馈两项）**：①会话列表点项目组头未设置工作上下文（ADR-0054 漏接一处入口）——组头点击补调 followProject（不导航），文件页即跟随；②HTML→Markdown 富文本标签切换卡顿实测坐实：生产构建下每次切回 ~515-567ms 且含单个 450-480ms 主线程长任务（300 行文档，Lexical 每次切换整树重建）——改为"切走隐藏不卸载"（缓存最近一个富文本实例，与 Monaco 面板同款持久化，文档关闭/退出富文本即释放），复测切换 79-96ms、长任务 0，首次进入仍 ~1s（一次性 chunk+首建）；markdown-rich-toolbar + m3-editor（5 例）e2e 回归全绿 | docs/adr/ADR-0053-work-item-document-page.md; docs/design/work-capture-directions/（四方向 mock）; docs/design/reminder-alert-directions/（动效方向 mock，含选定的 ★A+B） |
| 2026-08-08 | (transcript render-performance pass, ADR-0052, uncommitted — awaiting user acceptance) | `npm run check` + `npm test` + 11 相关 e2e spec + `tests/perf/ui/scroll-bench.ts` A/B + `tests/perf/ui/real-cli-bench.ts`（真实 claude/codex） | 起因：用户对照 ORCA 反馈长会话滚动卡顿。诊断出 8 项根因并全部修复：①主进程流式 delta 16ms 合并（终端输出早有合批，agent 文本一直裸奔；渲染层每 token 的 Zod 校验+结构化克隆+store 写随之消失）；②TaskRoomView 四个无 selector 全量订阅收窄（含一个从未读取的 workspaceStore 订阅——agent 写文件期间每 120ms 全房间重渲染）+ HomeShell/AgentPanel/SessionToolCanvas/卡片同治；③Markdown 加 React.memo + EventRow 行级 memo + TimelineContext 内容相等复用引用——一次工具跃迁不再全窗口重解析；④代码块流式期间延迟 Monaco 着色 + 有界 LRU 缓存 + 共享外观观察者；⑤滚动路径 rAF 合并（双读 scrollHeight 消除、吸底每帧一写、分栏拖拽每帧一写）；⑥.rt-bubble contain:content + 代码块 400px 限高；⑦动画收敛（历史里程碑圆点仅 live 脉动、发光去 inset 层、worklog 入场只动 opacity、SessionRail 分钟 tick 窗口隐藏即停）；⑧Atelier 拆全屏 mix-blend-mode/invert 保纹理（等效色差 <1/255）+ .tr-main 宽度过渡 160→90ms。**A/B 证据（400 事件重 markdown 转录，各 3 轮取中位）：一次带工具调用的 agent 回复落账时，主线程长任务总时长 10,044ms → 361ms（28×），Script 5,387→101ms，Layout 1,191→2ms，p95 帧 708→8.7ms，>33ms 坏帧 20.6%→0%**；渲染层收到的流式 IPC 事件减半（mock 1ms 节奏下 15→7）。真实端到端：真 claude 2.1.220 print 模式一次 32s 模型回合全程 4,176 帧 p95 9.2ms 零长任务，会话检测/落账/房间渲染/Resume 全链路正常；真 codex exec 检测+落账正常（截图+录屏于 tests/perf/ui/results/real/）。check 清（boundary 559）；单测全绿；11 个相关 e2e（转录层级/rail 样式/分屏画布/时间线窗口/权限卡/p2/m6/m8/atelier-skin/work-board-theme/memory）全绿。**对抗审查闭环**（独立 verifier，真实 Electron 复现装置）：抓到一个 CONFIRMED 缺陷——worker 崩溃路径绕过 delta flush，迟到的合并 delta 在 INTERRUPTED 后重建"幽灵流式气泡"（3/3 复现）；已双保险修复（onWorkerCrashed 先 flushStreamDeltas + 渲染层运行态门丢弃迟到 delta，回归单测 taskStore.stream-guard.test.ts），verifier 复验 3/3 PASS 且证实运行态门对 49 个正常 delta 零误伤（m7/m8/room-hierarchy 10/10）。第二发现：卸载瞬间的滚动保存对脱离节点测量 0/0/0 会误写"贴底"哨兵——防线下沉到 saveScroll 拒绝脱离/零高节点（scrollMemory.test.ts 5 例），惯性滚动最后一帧丢失（±一帧滚动量）作为已知代价记录于 ADR。最终 1358 单测全绿 | docs/adr/ADR-0052-transcript-render-performance.md; tests/perf/ui/results/*.json |
| 2026-07-24 | (Skills 用量漏计斜杠调用修复, landed) | `npm run check` + `npm test`(979) | 用户报:另一 Claude Code 会话刚跑过 web-access,Skills 表计数仍停在 20。实锤:`/web-access` 斜杠调用在 transcript 里记录为 `<command-name>/web-access</command-name>` 的**用户条目**(无 Skill tool_use),`parseClaudeTranscript` 只认 tool_use → 整类斜杠调用不计数(全库 7 次 web-access 斜杠调用全漏)。修复:①解析器补 `commandInvocation()`——内容以 `<command-` 开头的用户条目提取 command-name 入 skillEvents(内建命令 /clear、/model 等也会产出原始事件,由既有 catalog join `joinExternalSkillEvents` 统一丢弃非目录名;正文中途提及标签的粘贴文本不计;sidechain 不计);chips 用的 `skills` 保持 tool_use-only 不引入内建噪声。②skillsStore.rescan() 补 `refreshUsage()`——此前点 Rescan 只刷目录不刷用量,数字看起来冻结。真库验证:修复后 45 天窗 web-access=21(+1,恰为当日 17:06 斜杠调用)。979/979 单测(+1 斜杠展开专项);check 清(boundary 416) | apps/desktop-main/src/services/session-archaeology.ts |
| 2026-07-24 | (模型选择弹层溢出修复, landed) | `npm run check` + `npm test`(978) + picker e2e | 用户报:网关 25 个模型时 composer 模型弹层顶出窗口——`.me-pop` 固定 `max-height:60vh` 且向上锚定,Home composer 居中时上方空间 <60vh → 弹层被窗口裁掉,列表滚动视口一半在屏外(其他模型不可达)、底部 effort chips 也被裁。修复(ui.tsx + home.css):打开时测量触发按钮上方实际空间,内联 `max-height = min(top-16, 60vh)`;弹层改 flex 列,模型行移入独立滚动区 `.me-list`,Reasoning effort 区固定底部不随滚动。Playwright 断言 560px 矮窗下弹层完整在屏内;settings-focus picker 回归绿 | apps/desktop-renderer/src/views/ui.tsx |
| 2026-07-24 | (Provider 空回合吞错修复, landed) | `npm run check` + `npm test`(970) | 用户报「选 gpt-5.4 没反应」:回合显示 Answered 但零文本、Run details 空。三层实锤(网关 /v1/models 广告 14 个它不在 anthropic 协议上服务的 gpt/gemini 模型;非流式 404;**带 tools/system 的流式请求返回 HTTP 200 + 非标 SSE 帧 `data:{"type":"error","error":"model: gpt-5.4"}`(error 为字符串)**→ Pi 以干净空 end_turn 收场,adapter 的 sawError 需要 errorMessage 存在 → 静默 Answered;app.db 事件流与本地桩复现完全一致)。修复(agent-runtime-pi):①空回合守卫——run 结束但整回合零产出(无 message/thinking/tool/delta、零输出 token)→ `run.failed` AG_PROVIDER_ERROR 带诚实提示「模型未返回任何输出,该端点可能不服务此模型」;②sawError 放宽:stopReason=error 但无 errorMessage 也判失败。回归:`provider-error.test.ts` 两个真实网关错误形态(SSE 字符串错误帧/event:error 帧)各断言 run.failed 且无伪 Answered。check 清(boundary 415);970/970 单测(+2) | packages/agent-runtime-pi/src/provider-error.test.ts |
| 2026-07-24 | (Forwards 弹窗隧道图式 + Skills 表瘦身 + 传输层加固, landed) | `npm run check` + `npm test` + SSH/skills e2e(`--config tests/e2e/playwright.config.ts`) | ①用户嫌 Forwards 弹窗丑 → A/B mockup 确认选 **A 隧道图式**(ADR-0047 修订 2026-07-24c):主机进副标题、空态教学示意图、隧道 composer(本机端口 ─⚿主机─▶ 目标)、开关、呼吸灯转发行;testid/行为不变。②Skills 表按用户指令删「Charter context (per turn)」与「Decision」两列(Manage 保留),连带清 groupDecision/contextTokens/「Highest Charter context」排序项与 CSS 死代码。③两处真加固:disconnect 会话残留竞态(channel close 事件依赖服务端回包、client.end() 抢拆 socket → manager 持 per-channel finalize 同步终结,回归单测证明旧实现不可过);fake-sshd dropConnections 改销毁 raw socket(真断网语义,优雅 end() 高负载下 close 无限延迟)。④教训:e2e 配置不在根,裸 npx playwright 跑=默认并行 workers → 多 Electron 互相饿死出幻影失败;必须 `--config tests/e2e/playwright.config.ts`;launchApp 加 stdio 排水。check 清(boundary 412);**956/956 单测**(+1 finalize 回归);SSH e2e 9/9(×3 repeat)+ skills 3/3;截图与 mockup A 对照通过 | docs/adr/ADR-0047-ssh-connection-manager.md(2026-07-24c);docs/design/ssh-mockups/forwards-dialog-redesign.html |
| 2026-07-24 | (SSH Remotes Files 面板改版:双栏 + 全局传输中心, landed) | `npm run check` + `npm test` + `npm run test:security` + SSH e2e | 用户从三个交互范式 mockup(双栏指挥官/装载篮/环境拖放+传输中心)选定融合版:**A 双栏为主体 + C 的全局传输中心**(ADR-0047 修订 D13-D17)。左「This Mac」本地栏 ↔ 传输闸(›/‹)↔ 右远程栏;click/⌘/Shift 多选、跨栏拖拽(内部 mime 标记)与 OS 文件拖入共存、双击目录进入/双击文件即传;新 `LocalFilesService` + `ssh.localHome`/`ssh.localList`(仅元数据,字节不过 IPC,`~` 展开、相对路径拒绝);`ssh.sftpDownload` v2 `localDir` 定向下载 + 同名 `name (1).ext` uniquify 绝不覆盖;`ssh.sftpRetry` 用主进程留存端点重发(终态保留 10min,端点从不进渲染层);面板内嵌传输条删除,新全局传输中心挂 Workbench 根(胶囊聚合 进行中/百分比/渲染层差分平滑速率,弹层按主机分组,取消/重试/清除已完成,跨 surface 常驻)。check clean(boundary 412);**955/955 unit**(+6:LocalFilesService×3、localDir uniquify、retry 重发/拒绝);security 148+2+secret-scan 全绿;SSH e2e 3/3(第三条升级为双栏真回路:上传字节断言 + 下载 uniquify 磁盘断言 + 传输中心胶囊/弹层/清除);追加可编辑路径栏(两栏面包屑点空白/铅笔进编辑,Enter 直达、Esc 取消,远端 `~` 依服务器 home 展开,e2e 改用路径栏导航覆盖);再追加**远程会话收敛 shell-only**(用户决定:卡片 caret 菜单移除、New Terminal 对话框远程 target 锁 shell,引擎 launch×target 保留仅撤 UI 入口,README 中英同步去夸大表述,e2e 断言菜单不存在) | docs/adr/ADR-0047-ssh-connection-manager.md(2026-07-24 修订);docs/design/ssh-mockups/upload-*.html |
| 2026-07-23 | (SSH Remotes PR2+PR3 + acceptance fixes, landed) | `npm run check` + `npm test` + `npm run test:security` + `playwright test` (full e2e) | Acceptance feedback + PR2/PR3 on top of PR1 (ADR-0047 revision). **Lifecycle fix:** idle-disconnect cut 5min→10s and gated on a hold refcount (SFTP channel / active forward / dependent jump), so a card returns to `Connect` promptly once its last session exits — and `disconnect()` now closes every channel explicitly so multi-session teardown is deterministic; transport auto-reconnect only fires while a hold wants it. **Card redesign:** dropped standalone Open Claude/Codex buttons for a Connect / **+ New Session** split-button (caret picks shell/Claude/Codex) — one host multiplexes N sessions, listed and focusable on the card. **PR2 SFTP:** same-connection sftp channel, byte streams stay in main (fs↔sftp `stream.pipeline`), `ssh.sftp*` channels + throttled `ssh.sftpProgress`; drag-drop upload (OS `pathForFile`), download via save dialog, cancel via AbortSignal, bounded recursive delete (≤2000/≤16, no symlink follow); embedded file browser. **PR3 forwards + ProxyJump:** local `-L` forwards (net.Server + forwardOut, listener survives transport drop → reconnect-on-demand, persisted in `settings.ssh.hosts[].forwards`, `ssh.forwardState` broadcast); single-hop ProxyJump (jump is a first-class connection with its own host-key/auth, held open by its dependent). An adversarial verifier pass then caught a **CONFIRMED** defect (fixed): calling `disconnect()` during an in-flight handshake (auth prompt open / slow connect) left the stale-generation `ready` handler neither ending the client nor settling the promise → a leaked authenticated connection + that host's `connect()` wedged forever. Fixed with a `superseded()` path (end client + reject once), a `connectPromise` identity guard, disconnect clearing the in-flight attempt, and a `gen`-guarded reconnect `.catch`. Two PLAUSIBLE issues also fixed: a forward-start race (mid-start disconnect leaked a listener/hold — now a cancel token torn down at both checkpoints) and a frozen `autoReconnect` toggle (now the per-target settings gate decides at connect time); plus a card-state churn nit (stamp lastConnectedAt on the connected edge only). check clean (boundary 409); **949/949 unit** (+24: idle/hold/direct-tcpip/ProxyJump/multi-hop-reject/handshake-disconnect in ssh-service, SFTP session + service, forward service incl. cancel-mid-start); security 148+2 (secret-not-detectable now walks `secrets/ssh/`); **full e2e 151 passed / 20 skipped / 0 failed** incl. 3 SSH specs (first-connect→drop→card-back-to-Connect; multi-session card; SFTP list+mkdir + forward TCP round-trip) | docs/adr/ADR-0047-ssh-connection-manager.md (2026-07-23 revision); tests/e2e/ssh-remotes.spec.ts |
| 2026-07-23 | (SSH Remotes PR1, landed) | `npm run check` + `npx vitest run` + `npm run test:security` (vitest half) + `playwright test ssh-remotes` | SSH connection manager (方案 B, ADR-0047): ssh2@1.17.0 pinned (cpu-features excluded from the packed app → pure-JS fallback), new `@pi-ide/ssh-service`, TerminalManager backend seam (PtyBackend + adoptBackend, pure refactor — 39 terminal-service tests green), host book in `settings.ssh` + keychain vault (`SshVaultService`), host-key TOFU + `~/.ssh/config` import, remote shell / Claude / Codex sessions in the rail, Remotes surface + host dialog + TOFU/auth modals. check clean (boundary 399); **925/925 unit** (+5: connection-manager loopback, host-keys, ssh-config, adopt-backend, vault); security 148/148 incl. new `ssh-secret-boundary` (no `ssh.*` response/event carries a secret) + vault disk-isolation, secret-scan clean; SSH e2e passes end-to-end against a loopback ssh2 sshd (New Host → TOFU fingerprint modal → password → live remote session → connection-loss ends session). e2e caught a real bug: RemotesView opened sessions via raw IPC, bypassing the terminal store — fixed to route through `useTerminalStore.create` | docs/adr/ADR-0047-ssh-connection-manager.md; tests/e2e/ssh-remotes.spec.ts |
| 2026-07-22 | 4bf161b (tag v1.0.0-beta.2) | Unsigned Preview Release run 29889057023: release-gates (check/unit/perf/e2e/security/soak/dependency-safety/audit) → package ×3 → publish | ALL GREEN — mac-arm64 dmg+zip, windows-x64 nsis, linux tar.gz + SBOM/SHA256SUMS/manifest/GATE_REPORT/third-party notices published (13 assets). Acceptance audit found a later `prerelease:false` / `Latest` metadata change; it was corrected without replacing tag/assets and reverified at 2026-07-22 23:27 CST as `prerelease:true`, `draft:false`, title “Unsigned Preview”, 13 assets. En route: ① beta.2 version bump exposed hardcoded `v1.0.0-beta.1` in m1-baseline + packaged specs (e72f887 reads package.json now); ② fresh fast-uri advisory GHSA-v2hh-gcrm-f6hx failed the audit gate — lockfile entry hand-bumped 3.1.3→3.1.4 (4bf161b) because `npm audit fix`/`npm update` re-resolve the tree and strip the pinned nested brace-expansion/protobufjs safety copies; ③ site + brand rule updated for downloadable beta (charter-site 081837b) | https://github.com/longyunfeigu/Charter/releases/tag/v1.0.0-beta.2 |
| 2026-07-22 | (release unblock → v1.0.0-beta.2) | root-cause from CI #42/#43 + tag-run gate-reports → `npm run check` + `npm test` + `playwright test external-cli m10-recovery shell-v4` | Three fixes unblocking the Unsigned Preview Release. ① AgentHost restart races: worker exit now rejects that spawn's ready handshake (a worker killed mid-spawn — e.g. `secrets.set` → `stopWorker` — stalled every joined `ensure()` for the full 15s timeout and left models.list empty with no recovery broadcast); joiners retry a failed joined spawn once; `stopWorker`/exit/ready handlers operate on the captured child so a stale exit can't tear down or falsely vouch for a successor. ② external-cli composer spec: argv assertions tolerate the terminal-control wrapper's injected `--mcp-config=…` prefix (571659b intentional behavior; unit-covered in terminal-control-integration.test.ts). ③ shell-v4 heartbeat/focus spec reordered — live-board assertions run inside the 60s HEAT_WINDOW_MS before rail checks/screenshots; m10-recovery model waits get 30s cold-start headroom. check clean (boundary 367); 832/832 unit; targeted e2e 15/15. Version bumped to 1.0.0-beta.2 (beta.1 tag failed its gates and was never published; policy requires tag == v{version}), RELEASE_NOTES updated | gate-reports artifact of run 29822198940; tests/e2e traces |
| 2026-07-21 | (M12 release candidate) | `npm run check` + `npm test` + full E2E/security/perf/soak + package/install/package-E2E + dependency audit | check clean (boundary 346); 805/805 unit; 138 E2E passed + 19 conditional skips; security 139+2; perf 6/6; soak 50 laps; npm audit 0; unsigned mac DMG installs and launches; E2E-023/024 pass | `docs/TEST_REPORT.md`, `release/gate-report.json` (generated, not committed) |
| 2026-07-21 | (clipboard image card, landed) | `npm run check` + `npx vitest run` + `npm run build` | check clean (boundary 334); 758/758 across 88 files (10 new: clipboard watcher 9 / announce funnel 1); build clean. Test-driven fix: name recycling after FIFO eviction would collide with the announce dedupe set — session-unique names now enforced | docs/adr/ADR-0039-clipboard-image-card.md |
| 2026-07-20 | (screenshot quick card, landed) | `npm run check` + `npx vitest run` + `npm run build` | check clean (boundary 320); 715/715 across 83 files (29 new: watcher 7 / handlers 9 / channels 2 / store 4 / feed 7); build clean; watcher tests ×3 standalone green after FSEvents-lag nudge hardening | docs/adr/ADR-0036-screenshot-quick-card.md, docs/design/screenshot-quickcard-mock.html |
| 2026-07-20 | (replay v3.2 lean recap, landed) | owner before/after mock approved (`docs/design/replay-v32-lean-recap-mock.html`, 同一 add-函数会话 13 行→5 行) → `npm run check` + `npx vitest run` 667/667 + `playwright test replay-v3 replay-semantic-ui p2-parallel-replay` (5/5, 3/3, 3/3) | Replay V3.2 lean recap per ADR-0035 — 五条投影规则，账本/Explore/Verify 不变：① fold bar 要求 ≥FOLD_MIN(3) 条实质记录，纯心跳跨度归 story-footer 汇总（每条记录恰好呈现一次：行/贴纸/条内/footer 计数），<3 条内联；② 紧凑随规模涌现（小会话自然零折叠条，10k 探索段折叠条保留）；③ 新 `resolves` 关系（permission requestId→pending→callId 链；plan 版本 join，activity 投影两侧写 `plan-v{version}` parentKey）驱动"✓ 你批准了/自动批准"chip 贴上目标行，denied/join 失败 fail open 保持独立行；④ read/search/state/system 错误 + outcome=completed → amber 软提示行（"过程性错误 · 会话仍完成"，原始码降小字），command/write/verification 失败保持红色；⑤ '结构化记录' 默认徽章从故事行移除，只标例外等级。Shared-tree note: p2 PIVOT-018 一度红——并行会话中间态所致，归因验证（stash mine→仍过；重建全量→3/3 绿） | docs/adr/ADR-0035-replay-v32-lean-recap.md |
| 2026-07-20 | (memory icon + stale-skin cleanup, landed) | icon candidates mockup `docs/design/memory-icon-options.html` → user picked A (brain) → `npm run check` + build + in-app screenshots (rail + ⌘K) | Memory surfaces now use a `brain` icon (new `home-icons` path; SessionRail rail button, QuickLauncher "Open Memory" action + Memory search hits) — the previous `archive` glyph collided with the row-level Archive affordance. Root-caused the user's "dark shell / white dialog" screenshot: an HMR-ghost window still carrying `data-skin='atelier'` (a 方案Y skin removed by the revert) — current code follows the theme correctly (verified computed `--bg-card` in both modes); cleaned the stale invalid `"skin": "atelier"` from the live settings.json (source of the `general.skin invalid` boot warning) and restarted dev clean | — |
| 2026-07-20 | (rail history + external resume, landed) | mockup `docs/design/session-history-external.html` user-approved (规则确认：活着的永不进 History；accepted 进 History 且可 Resume 续聊为新任务) → `npm run check` + `npm run build` + `npx vitest run` + `playwright test external-cli m6-agent-ask` + in-app screenshots | Rail History now admits product-launched external CLI sessions once they are over AND decision-free: predicates `isHistoryTask`/`canResumeExternal` in labels.ts (unit `session-history.test.ts` 6/6 — alive-never-History, unreviewed-changes-never-hidden, settled/answered in), exited bare claude/codex terminal rows too; rows carry a hover ↻ Resume beside Archive (`.sr-actions`). `external.resumeSession` v2 returns the owning `taskId`: unsettled states keep the same-task resume; a settled source (ACCEPTED/ROLLED_BACK/CANCELLED) continues the SAME CLI conversation as a NEW task — fresh entry snapshot via `snapshotTree()`, carried `sessionId`, cross-linked `external.sessionContinued`/`sessionResumedFrom` events, undetected-CLI stubs auto-archived (worktree-mounted stubs kept — archive would discard the shared mount); renderer follows the response into the new room. 607/607 unit; external-cli 7/7 (new: accepted → History → ↻ → new task IN_PROGRESS, old row stays ACCEPTED; composer-launch spec adapted — ended answered session now lives under History) + m6 4/4; check + build clean. Shared-tree note: tree briefly broken by the parallel single-tree refactor mid-flight (deleted views with stale importers) — waited it out, converged green | ADR-0017/ADR-0023 (History + settled continuation) |
| 2026-07-20 | (single-tree, landed) | `npm run check` + `npm test` + `playwright test` (two batches: restructure/m3/m5/room-context-feeding/pivot-shell/shell-v3/home-dragref/session-rail-polish; m4/m10/p3/p4/shell-v4/session-canvas/readme-assets/site-assets2) | check clean (boundary 303); 607/607 unit; e2e 23/23 + 23/23 green (3 CHARTER_SHOTS-gated screenshot jobs skipped by design). Unit run shares the tree with the parallel session — verified green after its ipc-contracts edits converged | docs/adr/ADR-0029-single-project-tree.md |
| 2026-07-20 | (session-4b, landed) | owner mock rounds ×2 (`docs/design/session-4b-continuous-room.html`; round 2: pure conversation stream, ledger in the rail) → `npx vitest run` 649/649 + `npm run check` + e2e batches: m1–m10 core, p2/p3, shell-v3/v4, room-\*, preview-gate, session-\*, external-cli, replay suites, memory/skills/timeline-window, soak — all green after spec rewrites to the amended §6.1; screenshot walk 4b-turn\*.png | Session-as-conversation (ADR-0032): the Claude-Code-shaped model — one Session hosts chat + work forever; settlement is per turn (`agent_runs.review_state`, migration v6 with historic-row conversion), IDLE is the settled resting state, ARCHIVED the only close (worktree merge-back moves to archive). Per-turn accept + newest-first per-turn rollback (`task.rollbackTurn`, byte-exact boundary restore via new ChangeService.rollbackToStates). Rail gains the Turns ledger (removed next day at acceptance — see the 4b acceptance row); the conversation stream stays free of ceremony. Follow-up-as-new-Session deleted for live rooms (archived rooms keep continuation). Known pre-existing failure documented: m2-shell diagnostics overlay (parallel ADR-0029 rework), not this change | docs/adr/ADR-0032-session-as-conversation.md |
| 2026-07-20 | (session-4b acceptance round, landed) | owner in-app review with screenshots → fixes verified by `npm run check`, 649/649 unit, `playwright test room-context-feeding shell-v4 m6-agent-ask` 11/11, screenshot walk (`/tmp/charter-replay-semantic/at-picker-above.png`, `summary-no-turns.png`) | Three acceptance corrections. ① Rail Turns ledger removed (owner: no turn detection — summary stays session-level like Claude Code/Codex); `task.turns`/`task.rollbackTurn` remain API-level channels without a UI entry (ADR-0032 amendment 1). ② Answered Done ceremony deleted + `task.accept` made idempotent — repeated accepts on a settled/zero-change Session no longer append "Changes accepted" ledger rows (owner hit 9 duplicates); regression e2e double-fires task.accept and asserts zero events (ADR-0032 amendment 2; shared-tree convergence: dock/service/spec landed by the parallel session, bridge-idiom typecheck fix + docs by this line). ③ Room @ file picker un-clipped: the pop-over now anchors ABOVE the bottom-docked composer in both composer variants (pre-existing CSS regression from the committed ADR-0024/0029 line — the upward override never reset `.hm-menu`'s downward `top`; follow-variant menu moved out of `.hm-chiprow` so both anchor to the card) | docs/adr/ADR-0032-session-as-conversation.md（amendments）|
| 2026-07-20 | (replay v3.1, landed) | two owner mock rounds (`docs/design/replay-v4-recap-mock.html` revised after duplicate-check against the room, `-noncode.html` for the zero-file-change persona) → `npm run check` + `npx vitest run` (619/619) + `playwright test replay-v3 replay-semantic-ui p2-parallel-replay external-cli` (5/5, 3/3, 2/2, 7/7) | Replay V3.1 conversation-first recap implemented per ADR-0031: quoted conclusion line (verbatim report excerpt + citation chips, Inferred-labeled), read-only return line replacing the mock's action panel (room review bar is the single entry, ADR-0016), fold placeholders (no silent story holes), pivot detection + violet cards + `pivot` chapter (recorded plan revisions only; ticks/lifecycles excluded), outward-action dual track (recorded `app` only, never name-inferred; irreversible/high-risk pinned in attention; result sentence de-file-centered for non-coding users), inputs ledger cell (user code refs; memory/rules honestly unrecorded), message bubbles with bounded full prose, parser TodoWrite/ExitPlanMode/todo_list → plan. Cost cells cut (RunDetails owns usage) | docs/adr/ADR-0031-replay-v31-conversation-first-recap.md |
| 2026-07-20 | (memory file dialog, landed) | mockup `docs/design/memory-file-dialog.html` user-approved (View 弹窗渲染 Markdown / Edit 原文；View 态无底部 Close) → `npm run check` + `npm run build` + `playwright test memory` + in-app screenshots | External-file View/Edit moved from inline card expansion (mono `<pre>` capped at 320px, long lines clipped) to a nested centered dialog over the memory overlay: View = frontmatter chip strip + shared `Markdown` renderer (`[[wiki-links]]` as inline-code chips, fenced blocks untouched), Edit = raw file text with Save/Cancel (mtime conflict + truncated→Save disabled preserved), View⇄Edit segmented switch in the header, Escape intercepted capture-phase so only the file dialog closes (outer memory overlay stays). check + build clean; memory e2e 3/3 (spec extended: dialog visible, frontmatter chips rendered, ✕ close, Esc closes dialog but not overlay) | ADR-0028 (UI refinement during acceptance) |
| 2026-07-19 | (memory ia-v3, landed) | mock iterations `docs/design/memory-ia-v2.html` → user rejected → `memory-ia-v3.html` approved; then `npx vitest run` + `npm run check` + `npm run build` + `playwright test memory` | Memory panel restructured to the user's mental model: agents (Claude Code/Codex/Charter) are the top level, each = GLOBAL memory + PROJECTS second level; new `memory.tree` channel + `ExternalMemoryStore.listAll` (full ~/.claude/projects scan, munge-matched workspaces named, foreign dirs verbatim browse/delete-only); Charter groups lazy-load rules/candidates/Distribution per project; every store mutation names its project (no focused-workspace dependency). 594/594 unit (+2: listAll grouping/matching, agentsTree counts); check + build clean; memory e2e 3/3 re-adapted | ADR-0028 amendment (IA v3) |
| 2026-07-19 | (memory, landed) | `npx vitest run` + `npm run check` + `npm run build` + `playwright test memory p4-review-v2 m8-review m6-agent-ask skills session-workbench` | 591/591 unit (47 new memory; privacy + persistence regressions extended — `privacy.clearHistory` now clears task-derived memory rows, would have hard-failed on the injections FK otherwise; migration upgrade test asserts [4,5] + four memory tables); prettier + boundary(305) + tsc clean; build clean; memory e2e 3/3 (distill loop with on-disk rules file + injection counter after the next run; AGENTS.md drift walk with fs assertions and no silent overwrite; fake-home external memory incl. one-way promote + backup-first delete); adjacent regressions 16/16 | docs/adr/ADR-0028-project-memory.md |
| 2026-07-19 | (external-launch-intents am.2, landed) | controlled real-claude PTY experiments (nested-env vs stripped-env, interactive vs print) + `npx vitest run` + boundary/tsc/build + `playwright test external-cli` | Root cause of field resume failure isolated to nested Claude Code env markers suppressing interactive transcript persistence; PTY env sanitization verified (stripped env → live transcript under the pre-assigned uuid; `--resume <uuid>` continues it). 544/544 unit, external-cli 6/6, check gates clean | ADR-0017 amendment 2026-07-19 (second) |
| 2026-07-19 | (external-launch-intents, landed) | `npx vitest run` + `node scripts/check-boundaries.mjs` + `tsc --noEmit` + `node scripts/build.mjs` + `playwright test external-cli` + `playwright test session-rail-polish terminal-remount m4-search-lsp-terminal shell-v4` | 537/537 unit (20 new/updated: launch-intent registry, title derivation, launch-command UUID guard, presentedMeta external-awareness); boundary 285 + tsc clean; external-cli 6/6 incl. new composer-launch walk (pre-assigned `--session-id` uuid, host-delivered first prompt proven by canonical-mode PTY line release, session titled by first message, `Ended` presentation, `--resume <same uuid>`); adjacent regressions 15/15. Global `prettier --check .` fails only on parallel session's untracked `tests/e2e/site-assets2.spec.ts` (not ours); all touched files prettier-clean | docs/adr/ADR-0017 amendment 2026-07-19 |
| 2026-07-18 | (M11, landed) | `npm run test:security` + `npm run test:perf` + `npm test` + `npm run check` + `npm audit` | M11 complete (7 tasks, ADR-0025 rescope + ADR-0027 impl). test:security green (repo secret-scan → 137 vitest/14 files → build → 2 Electron specs: shell-hardening CSP/nav/window.open/webview matrix + four-path secret-not-detectable via CDP heap snapshot). test:perf 6 green (tree 50k p95 7.4ms FULL, quick-open p95 12ms, search first-200 p95 248ms, projectReplay-10k p95 29ms, oversized-file 6ms, pre-abort cancel <1ms). 524 unit (+22: shell-policy/preview-boundary/secret-scan/perf-lib/timeline-window 9/ui-zoom 5/accessible-diff 5/privacy-service 3). check clean (boundary 283). e2e added: timeline-window, a11y-zoom-diff, privacy-settings — all green; room/session-canvas/settings regressions green. npm audit: 1 low + 1 moderate (DOMPurify via monaco-editor, no setConfig usage + strict CSP → not reachable), 0 Critical/High. Deferred to M12: packaged cold-start/paint/memory numbers, SBOM/licenses | docs/M11_SECURITY_PERFORMANCE_REPORT.md, docs/adr/ADR-0027-m11-hardening-implementation.md, docs/design/m11-functional-mockups/impl-shots/ |
| 2026-07-18 | (m11-rescope, docs-only) | Gap inventory at `e81b72e` (grep/file evidence) → ADR-0025 + backlog M11 rewrite + M10 status sync + DECISIONS index backfill | M11 re-scoped as gap-closure: ids/gates M11-01..06 kept, M11-07 privacy added (PRIV-001..003 was missing from the original list; `settings.privacy` toggles have zero consumers outside schema/SettingsView and the Crash-reports hint describes a nonexistent redacted preview — rule-9 debt booked to M11-07). Verified-absent gaps: @electron/fuses, UI zoom (A11Y-003), accessible diff (A11Y-005), 50k/1GB fixture, `vitest.security.config.ts` + `tests/security/` + `vitest.perf.config.ts` (both npm entries non-runnable); RoomTimeline (1309 lines) unvirtualized — repo-clean memoization removed per-token rebuilds but is not windowing. Verified-present base: security.ts/csp.ts, redact-by-default logger, redacted support bundle, scattered traversal/symlink tests, Explore virtualization, 10k fixtures, output truncation. Pivot surfaces folded in: preview iframe/picker injection (ADR-0022), external CLI transcript reads (ADR-0017), provider keychain (ADR-0009 am.1), task attachments (ADR-0024), Session Rail/RoomTimeline as the only shell (PIVOT-037). Backlog M10 rows synced to VERIFIED (were stale NOT_STARTED); DECISIONS.md backfilled ADR-0022/0023/0024 index rows | docs/adr/ADR-0025-m11-rescope.md |
| 2026-07-18 | (repo-clean, 10 commits d22f081..) | 6-agent parallel audit → per-batch `npm run check` + `npm test` + affected e2e | Cleanup sweep, zero feature change: untracked demo npm cache (787 files ~220MB) + root .DS_Store, gitignore hardened; dead code removed with grep evidence (renderer: openInEditor.ts, Splitter.tsx + its orphan `.splitter` CSS, LANE_LABEL; packages: foundation/clock.ts, withTimeout/isAbortError/abortFailure, memorySink/consoleSink, isTerminalState, isKnownEventChannel, scanDescendantsForAgent, createNonGitFixture; dead skipped e2e sidebar-vnext-mock.tmp.spec.ts); deps: dropped unused @electron/rebuild + @xterm/addon-search, prismjs declared explicitly (was phantom via @mdxeditor); boundary checker now sees aliased requires (`require_`) with an explicit package.json-metadata exemption (+2 unit tests); perf: RoomTimeline/TimelineList derivation memoized + TimelineCard React.memo (no per-token rebuild), SessionRail narrowed to `s.tasks` slice, idle 1s tickers gated (ActivityStrip, TerminalRunStatusItem); duplication: foundation `errorMessage()` (52 sites/24 files), `okOrToast()` (16 strict sites), AgentMode/VerificationCommand single-sourced, xmlAttribute + image-MIME twins merged. Kept on purpose: getM5/structuredPatch (TODO_M8_M12 prepared seams), settings.reset/task.pendingPermissions channels, writeFileAtomic variants (fsync/chmod semantics differ). 502/502 unit+integration, check clean per batch; affected e2e 11 passed, 1 pre-existing failure (room-conversation-hierarchy bubble x-position) reproduced identically on baseline d22f081 | git log d22f081..HEAD |
| 2026-07-18 | (session-split-drag, landed) | `npm run check` + `npm test` + build + `playwright test session-canvas code-context room-peek` | Session canvas split is draggable (user-approved mock A of docs/design/session-split-mockups): new SessionSplitHandle between conversation and tool canvas — hover grip, live dark-chip readout (对话 % · 工具 % · px), min 380/360 clamps with at-limit flash, double-click resets 56/44, ←/→ nudges 2%, ratio persisted per Session (charter.sessionSplit.\*); a manual ratio overrides both stops so the Diff auto-expand no longer shrinks a hand-widened conversation, while the Expand button explicitly clears it (stop jump); handle hidden in the ≤1120px stacked layout. Two real bugs found and fixed during verification: (1) resting-width sync moved from the child's layout effect to TaskRoomView (child layout effects run before the parent host ref attaches on mount — reopening a room lost the remembered ratio), (2) lostpointercapture now ends the drag (a screenshot mid-drag stole the capture silently and left the handle stuck in dragging state). New e2e covers drag/chip/auto-expand-override/reopen-persistence/Expand/dblclick/keyboard; session-canvas 3/3, code-context 3/3, room-peek green, unit suite green | /tmp/split-impl-\*.png, docs/design/session-split-mockups/README.md |
| 2026-07-18 | (room-context-feeding, ADR-0024) | `npm run check` + `npx vitest run` + build + `playwright test room-context-feeding` + `CHARTER_SHOTS=1 playwright test ui-shots-context-feeding` + adjacent regression subset | Room context feeding parity (mock B+D, PIVOT-015r2/039r/040/041): session rail gains Sessions⇄Files segmented tabs — Files = persistent draggable tree (HomeProjectTree reused, hover quick-add, changed-file marks) + flat `search.files` results; Room references land as chips (`fileRefs` beside codeRefs, 12/4-image caps) — the `@` picker and tree drags no longer insert inline `@path` prose, one picker serves both composer variants; the whole room is one drop veil target (internal MIME + OS drops via `pathForDroppedFile`→`workspace.relativize`); out-of-project images import into `attachments/<taskId>/` via new `task.attachments.import` (10 MB cap, decode-validated `nativeImage` thumbs, path-escape guarded) and ride `host.steer/followUp/startRun` `images`; clipboard paste → image chips; prompts gain the `<file_context>` data block (anti-injection framing mirrors code-context); `user.message` payloads record sanitized refs and the timeline renders read-only sent chips; ExplorerView rows are drag sources now. 501/501 unit (+22: contracts schema/formatter, import/resolve service incl. escape+missing-attachment failures, draftStore caps), check clean, room-context-feeding e2e 2/2, real-Electron shots reviewed (Files pane / chips / drop veil / sent refs) | docs/adr/ADR-0024-room-context-feeding-parity.md, docs/design/context-attach-mockups/bd-fusion.html, /tmp/ui-shots/ctx-\*.png |
| 2026-07-18 | (code-context-float, landed) | `npm run check` + `npm test` + build + `playwright test code-context m3-editor room-peek session-canvas p3-light-edit p4-review-v2` | "添加到上下文" selection action rebuilt 1:1 against the approved mock (docs/design/code-context-ref-mockups shared.css `.selection-action`): static top bars in Diff/File Peek/Editor replaced by the dark anchored bubble (down caret, cream 添加到上下文 button, Chinese provenance labels 当前文件/Editor/已选 N 行·新版本), Diff rows get the mock's full-width `#9fc6f3` band and archive-light Monaco selection now uses the same mock blue; all testids and attach flows unchanged (479/479 unit, check clean, code-context 3/3; p4-review-v2:14 fails identically on clean HEAD — pre-existing, not from this change) | /tmp/charter-float-\*.png |
| 2026-07-18 | (projects-add-menu, landed) | `npm run check` + build + `playwright test home-dragref session-workbench project-files-restructure session-rail-polish` | Projects panel add-project redesign (user-approved mockup A of docs/design/projects-add-\*.html): footer "Open folder…/New project…" buttons removed; 34px "+" beside the project search (same geometry as the sessions filter button) opens a dropdown with icon+description items; no-projects empty state surfaces both entries in the panel body (`-empty` testids); outside-click/Escape close; check+tsc clean, 10/10 e2e, menu/default screenshots reviewed | docs/design/projects-add-a-header-plus.html, test-results/projects-panel-\*.png |
| 2026-07-17 | (session-canvas, landed) | `npm test` + `npm run check` + build + `playwright test session-canvas external-cli` | 470/470 unit; check/build clean; Session Canvas 2/2; all 5 external terminal scenarios green after migration; real Electron at 1440×900 and 900×900, zero renderer console/page errors | `/tmp/charter-session-canvas/*`, `design-qa.md` |
| 2026-07-17 | (rail-marks+starters) | `npm run check` + build + `playwright test session-workbench` (3, incl. new starters spec) + `CHARTER_SHOTS` rail walk | ADR-0023 am.1: brand logomarks (Claude/Codex/π) replace letter chips, bare row titles (no `Provider ·` prefix, no hardcoded CLI-name titles), Projects-panel hover starters (π context+composer; Claude/Codex bound via `kind:'recent'`); check+tsc clean, 3/3 e2e, light/dark screenshots reviewed | /tmp/ui-shots/rail-icons-\*.png |
| 2026-07-16 | (external-session-ids, landed) | `npm test` + `npm run check` + `playwright test external-cli session-workbench` | 470 unit passed (9 new locator/command tests); check + build clean; e2e failure set identical to clean HEAD (4 pre-existing dock-surface failures, 0 new) | vitest+playwright |
| 2026-07-16 | (rail-d, landed) | `playwright test session-workbench home-v2 home-dragref shell-v4 skills` | 17 passed, 1 failed (home-v2 PIVOT-012/015 — pre-existing, reproduces on clean HEAD with the rail files checked out to HEAD; advanced-criteria loss is in the composer/charter path, untouched here) | playwright |
| 2026-07-16 | (rail-d, landed) | `playwright test pivot-shell shell-v3 p3-full-mode external-cli` + tmp screenshot walk | 12 passed, 4 failed — all 4 in external-cli (editor terminal dock surfaces); representative 2 re-run against a clean-HEAD build fail identically → pre-existing from the earlier shell rework, unrelated to the rail | playwright |
| 2026-07-16 | (rail-d, landed) | `npm test` + `npm run check` | 461 unit passed; prettier + boundary + tsc clean; production build clean | vitest |
| 2026-07-16 | (rail-d, landed) | screenshot walk (temp spec, removed) | sessions grouped view (needs badge, B-style selection, context footer), inbox panel, projects panel + tree reviewed | /tmp/ui-shots/raild-\*.png |
| 2026-07-16 | (preview-room am.2 fix, landed) | `playwright test room-preview` | 3/3 — new "one-click dev start keeps you in the Room, no surface flip to the editor" regression + existing two. Field report: starting the dev server yanked the user into the Editor terminal, then back. Root cause: `terminal.create` default `reveal:true` → `showBottomTab` → `setSurface('workspace')`. Fix: start the dev server in a background terminal (`reveal:false`); log reachable via an explicit "Dev log" button (Replit Console pattern — never a side effect of starting). | playwright |
| 2026-07-16 | (preview-room am.2, landed) | `playwright test room-preview preview-gate` + touched-surface regression | 2 + 5 + 11 passed (Full-mode rail reach, element-pick injection → composer chip → follow-up seed, console capture + manual steer, draw-region same-run fix; gate adopted shared Draw mode; p4/shell-v4/p3-full-mode/room-reviewbar green) | playwright |
| 2026-07-16 | (preview-room am.2, landed) | `npm test` + `npm run check` | 459 unit passed (9 new console-policy); prettier + boundary + tsc clean; production build clean | vitest |
| 2026-07-16 | (preview-room am.2, landed) | `CHARTER_SHOTS=1 playwright test ui-shots-room-preview` | 5-shot walk reviewed — Full-auto task shows a live preview rail + header badge (the exact blind spot the user hit), pick-armed hint, `#coupon-hint` selector chip in the composer, dark | /tmp/ui-shots/rp-\*.png |
| 2026-07-16 | (preview-gate, landed) | `playwright test preview-gate` | 4 passed — worktree/main-tree port attribution boundary, marquee→screenshot→image-through-worker round trip, accept→PR draft with git-side-effect-free proof, checks superseded history, non-web tab absence | playwright |
| 2026-07-16 | (preview-gate, landed) | `vitest command-classifier` + `playwright test m7-permissions p3-full-mode preview-gate` | 19 classifier unit (incl. forge R4/R3 split) + 10 E2E passed — gh/glab/hub mutations now R4 (GIT-007 class), reads stay R3; full-mode auto-allow can no longer publish | vitest+playwright |
| 2026-07-16 | (preview-gate, landed) | `npx playwright test` (full e2e) | 94 passed, 2 failed (E2E-002, E2E-005) — both reproduce on clean HEAD via `git stash` (pre-existing, unrelated: editor restore + TS intelligence), 9 skipped | /tmp/pi-e2e-full.log |
| 2026-07-16 | (preview-gate, landed) | `npm test` + `npm run check` | 449 unit passed (17 new: preview-service parsing/attribution, pr-draft builder, CSP pin); prettier + boundary(253) + tsc clean; production build clean | vitest |
| 2026-07-16 | (preview-gate, landed) | `CHARTER_SHOTS=1 playwright test ui-shots-preview` | 11-shot visual walk light+dark (gate tabs, honest empty state, live preview + isolation badge, armed marquee, note card, compact room feedback with burned-in selection, checks Unverified, PR draft card, timeline draft entry) reviewed; one usability fix applied from the review: feedback bubble leads with the note, coordinate block folded | /tmp/ui-shots/pg-\*.png |
| 2026-07-15 | (external-cli-rev2, landed) | `PI_IDE_REAL_EXTERNAL_CLI=1 playwright test external-cli-real` (manual, real CLIs) | 3 passed — REAL claude 2.1.210 interactive (nvm/npm shape, comm=claude.exe): detect-in-place bar → user promote → TUI renders ≥80col in 600px panel → typed probe echoed in composer → /exit → panel ended state → return-to-dock (bar ended); REAL claude -p haiku: edit accounted → REVIEW_READY; REAL codex 0.144.1 interactive: detect → promote → update-menu/trust gauntlet driven by keystrokes → probe echoed in composer | /tmp/live-e2e/\*.png |
| 2026-07-15 | (external-cli-rev2, landed) | `npx playwright test` (full e2e) | 76 passed, 0 failed, 9 skipped (3.5m) — includes rewritten external-cli.spec (decorate-in-place → user promote → PTY echo → no auto-return → return click → room/peek) and new terminal-remount.spec (tab A→B→A, Home⇄Editor round-trip) | playwright |
| 2026-07-15 | (external-cli-rev2, landed) | `npm test` + `npm run check` | 346 unit passed; prettier + boundary(212) + tsc clean | vitest |
| 2026-07-15 | (external-cli-rev2, landed) | Finding: prior native-installer e2e fixture (`/bin/zsh` copy) is SIGKILLed by macOS AMFI (platform-binary copy) — that spec could not have run its agent; replaced with a node-binary copy. Also: `external-cli.spec.ts:89` (promoted-scrollback assertion) was red at HEAD b7e787e — 决策 4 UI shipped against a failing gate | reproduced, then fixed | this row |
| 2026-07-15 | (room-preset-e, landed) | `CHARTER_TEST_KEY/BASEURL … playwright test real-room-e` (manual, real gateway) | 1 passed (1.1m) — real write task + follow-up run on anthropic/claude-haiku-4-5; files asserted on disk; preset-E styles asserted computed; focus-outline fix verified | /tmp/ui-shots/e-\*.png |
| 2026-07-15 | (room-preset-e, landed) | `playwright test room-reviewbar room-peek m6-agent-ask` | 8 passed (mock runtime, timeline regrouping regression) | playwright |
| 2026-07-15 | (room-preset-e, landed) | `npm test` + `npm run check` | 345 unit passed; prettier + boundary(211) + tsc clean | vitest |
| 2026-07-15 | (external-cli-detect-fix, landed) | `PI_IDE_REAL_EXTERNAL_CLI=1 playwright test external-cli-real` (manual, real CLIs) | 2 passed — REAL claude 2.1.209 (native installer, kernel comm = version string): detect ✳+EXT+toast → haiku `-p` edit accounted (e2e-touch.txt created) → REVIEW_READY; REAL codex 0.142.4 (user zsh-function wrapper → nvm shim): detect + external task armed | /tmp/live-e2e/\*.png |
| 2026-07-15 | (external-cli-detect-fix, landed) | `playwright test external-cli` | 2 passed — original lifecycle + new native-installer shape (version-named binary `fakeclaude → versions/9.9.9`, kernel comm ≠ CLI name, argv-fallback detection → accounting) | playwright |
| 2026-07-15 | (external-cli-detect-fix, landed) | `npm test` + `npm run check` | 345 unit passed (8 new: findAgentInTable + pollOnce gating); boundary(211) + tsc clean; changed files prettier-clean (an untracked design HTML draft fails repo-wide prettier, pre-existing) | vitest |
| 2026-07-14 | (external-cli, landed) | `npx playwright test` (full e2e) | 69 passed, 1 failed (p2 ⌘K keyboard — documented load-flake class, passes standalone ×1 right after), 5 skipped | playwright |
| 2026-07-14 | (external-cli, landed) | `playwright test external-cli` | 1 passed — full ADR-0017 lifecycle on a real PTY (detect → badge → room → accounting → peek → REVIEW_READY → honest changeSet) | playwright |
| 2026-07-14 | (external-cli, landed) | `npm test` + `npm run check` | 337 unit passed (16 new); prettier + boundary(211) + tsc clean | vitest |
| 2026-07-14 | (fix-init, landed) | `npm test` | 320 passed (init run-once 2 new) | vitest |
| 2026-07-14 | (fix-init, landed) | `playwright test m2-shell m3-editor` + dev relaunch | 5/5 passed; startup log clean of MaxListenersExceededWarning | playwright |
| 2026-07-14 | (room-ending, landed) | `playwright test room-reviewbar pivot-shell shell-v4 m6-agent-ask m9-verification` | 17 passed (2 new) | playwright |
| 2026-07-14 | (room-ending, landed) | `PI_IDE_SOAK=1 playwright test soak` | 1 passed — 50 tasks, rollback via review-bar overflow, one worker, 0 restarts | playwright |
| 2026-07-14 | (room-ending, landed) | `npm test` + boundaries + tsc | 321 passed (mock setSessionModel new); boundary(205)+tsc clean; prettier clean on all changed files | vitest |
| 2026-07-14 | (shell-v5) | `npm run test:e2e` (full) | 65 passed, 1 load-flake (m5-git E2E-008 — passes standalone ×2 and inside suite runs; documented flake class) | playwright |
| 2026-07-14 | (shell-v5) | `playwright test room-peek shell-v3 shell-v4 home-v2 home-dragref p2-visibility` | 2 new + 23 regression passed (worktree peek assertions added to shell-v4) | playwright |
| 2026-07-14 | (shell-v5) | `npm test` + `npm run check` | 303 passed (peek reducer 10 new); prettier+boundary+tsc clean | vitest |
| 2026-07-14 | (shell-v5) | `CHARTER_SHOTS=1 … ui-shots-v5` | visual walk pass (light+dark; peek diff/file/tabs). Known capture artifact: after an emulated mid-session theme flip, full-page CDP captures can show a stale light layer over the peek column — element-level captures and computed styles prove correct dark rendering (probe evidence); not user-visible app state | /tmp/ui-shots/v5-\*.png |
| 2026-07-14 | (ux-dragref, landed) | `playwright test home-dragref` + touched-area regression (home-v2, shell-v3, shell-v4, pivot-shell, p3-light-edit) | 3 new + 18 regression passed | playwright |
| 2026-07-14 | (ux-dragref, landed) | `npm run check` | prettier + boundary(190) + tsc clean | — |
| 2026-07-14 | (ux-dragref, landed) | screenshot walk (temp spec, removed) | sidebar entry, dialog, drag-over ring, file+dir chips, room inline @ reviewed | /tmp/ui-shots/dr-\*.png |
| 2026-07-14 | (p4) | `npm run test:e2e` (full) | 60 passed, 0 failed (soak/real-gateway/ui-shots gated) | playwright |
| 2026-07-14 | (p4) | `npm test` + `npm run check` | 293 passed; prettier+boundary(189)+tsc clean | vitest |
| 2026-07-14 | (p4) | `CHARTER_SHOTS=1 … ui-shots-v4` | visual walk pass — composer 4-mode segment verified compact | /tmp/ui-shots/v4-\*.png |
| --- | --- | --- | --- | --- |
| 2026-07-21 | (nav-sync, landed) | `playwright test project-files-restructure session-workbench m4-search-lsp-terminal` | 12 passed (incl. the startSession-ordering case the first run caught) | playwright |
| 2026-07-21 | (nav-sync, landed) | `npm test` | renderer suites green incl. NEW appStore.nav 12; 5 concurrent-session failures in skill-usage.test.ts (their in-flight byConsumer work, not nav-sync) | vitest |
| 2026-07-21 | (arch-time, landed) | `npm test` | 748 passed (87 files) incl. archaeologyStore +4 (day buckets/filter); also repaired 2 stale migration expectations in database.test.ts left behind by v8 | vitest |
| 2026-07-21 | (arch-time, landed) | `npm run check` | prettier + boundary(332) + tsc clean | — |
| 2026-07-20 | (fix-batch-0720b, landed) | `npx vitest run packages/ipc-contracts packages/persistence apps/desktop-main tests/unit` | 325 passed (41 files) incl. new migration-v7 / removeWorkspace / filesReset suites | vitest |
| 2026-07-20 | (fix-batch-0720b, landed) | `npm run check` | prettier + boundary(309) clean; tsc clean except 2 pre-existing errors in in-progress RecapDepth.tsx (untouched WIP) | — |
| 2026-07-14 | (ux-polish, landed) | `npm run test:e2e` (full) | 54 E2E passed (soak/real-gateway/ui-shots skipped) | playwright |
| 2026-07-14 | (ux-polish, landed) | `npm test` + `npm run check` | 268 passed; prettier+boundary(181)+tsc clean | vitest |
| 2026-07-13 | (shell-v3 P5) | `playwright test` (full) | 48 E2E passed, 0 failed (soak + credential-gated real-gateway skipped); singles that flaked under load pass standalone ×2 | playwright |
| 2026-07-13 | (shell-v3 P5) | real-gateway.spec (manual, real credentials) | PASS — key+baseUrl stored, 25 models fetched through the gateway, real ask task on anthropic/claude-haiku-4-5 answered with usage/cost from the pi runtime | /tmp/ui-shots/real-\*.png |
| 2026-07-13 | (shell-v3 P5) | `npm test` + `npm run check` | 263 passed; prettier+boundary(177)+tsc clean | vitest |
| 2026-07-13 | (shell-v3 P4) | `playwright test` (full) | 46 E2E passed ×2 consecutively (soak skipped); one earlier run flaked under heavy machine load only | playwright |
| 2026-07-13 | (shell-v3 P4) | `npm test` + `npm run check` | 262 passed; prettier+boundary(173)+tsc clean | vitest |
| 2026-07-13 | (M10) | `playwright test` (full) | 43 E2E passed ×2 (soak opt-in skipped) | playwright |
| 2026-07-13 | (M10) | `test:soak` (50 tasks) | 1 passed — one worker, 0 restarts, clean exit | playwright |
| 2026-07-13 | (M10) | `npm test` + `npm run check` | 253 passed; prettier+boundary(168)+tsc clean | vitest |
| 2026-07-13 | (home-v2 P3) | `playwright test` (full) | 40 E2E passed ×3 consecutively (logs /tmp/e2e-f\*.log) | playwright |
| 2026-07-13 | (home-v2 P3) | `npm test` + `npm run check` | 252 passed; prettier+boundary(167)+tsc clean | vitest |
| 2026-07-13 | (home-v2 P2) | `npm test` | 252 unit/integration passed (34 files) | vitest |
| 2026-07-13 | (home-v2 P2) | `playwright test` (full) | 38 E2E passed ×3 consecutively (one unidentified flake in an earlier run, not reproduced) | playwright |
| 2026-07-13 | (home-v2 P1) | `npm test` | 251 unit/integration passed (34 files) | vitest |
| 2026-07-13 | (home-v2 P1) | `playwright test` (full) | 35 E2E passed, twice consecutively | playwright |
| 2026-07-13 | (pivot) | `npm test` | 238 unit/integration passed (32 files) | vitest |
| 2026-07-13 | (pivot) | `playwright test` (full) | 32 E2E passed, twice consecutively | playwright |
| 2026-07-13 | (M9) | `npm test` | 234 unit/integration passed (31 files) | vitest |
| 2026-07-13 | (M9) | `npm run check` | prettier + boundary(147) + tsc clean | — |
| 2026-07-13 | (M9) | `playwright test` (full) | 29 E2E passed, twice consecutively | playwright |
| 2026-07-13 | (M8) | `npm test` | 226 unit/integration passed (30 files) | vitest |
| 2026-07-13 | (M8) | `playwright test` (full) | 26 E2E passed | playwright |
| 2026-07-13 | (M7) | `npm test` | 201 unit/integration passed (27 files) | vitest |
| 2026-07-13 | (M7) | `playwright test` (full) | 22 E2E passed (clean machine) | playwright |

## Requirement exceptions

None. Any exception requires an ADR and product-owner approval.
